# AgentCounsel — Skill Improvement Prompts

Generated by `scripts/generate_skill_improvement_prompts.py`. Do not edit this file by hand — re-run the script after adding or changing a skill.

This report contains one copyable improvement prompt for each of the 165 skills in the canonical library (`skills/`). Each prompt asks an AI coding agent to improve one specific skill while preserving its purpose and the AgentCounsel skill structure.

Adapter copies under `adapters/` are generated from the canonical skills and are not listed here. Improve the canonical skill, then run `python scripts/sync_plugin_skills.py` to refresh the bundle.

## How to use these prompts

1. Find the skill you want to improve in the section for its practice area.
2. Copy the entire fenced prompt block for that skill.
3. Paste it into an AI coding agent that has access to this repository.
4. Review the resulting diff with attorney-style scrutiny — keep only the changes that genuinely tighten the skill.
5. Run `python scripts/validate_repo.py` before committing, and regenerate this report so it stays in sync.

For a manual review pass, work through `reports/skill-quality-checklist.md` alongside these prompts.

## Skills by practice area

- [Legal Research](#legal-research) — 1 skill
- [Litigation](#litigation) — 9 skills
- [Contracts](#contracts) — 5 skills
- [Corporate](#corporate) — 6 skills
- [Employment](#employment) — 8 skills
- [Privacy](#privacy) — 4 skills
- [Product Legal](#product-legal) — 4 skills
- [Regulatory](#regulatory) — 4 skills
- [AI Governance](#ai-governance) — 4 skills
- [Intellectual Property](#intellectual-property) — 7 skills
- [Setup](#setup) — 5 skills
- [Legal Methodology](#legal-methodology) — 4 skills
- [M And A](#m-and-a) — 10 skills
- [Antitrust Competition](#antitrust-competition) — 10 skills
- [Family Law](#family-law) — 12 skills
- [Trusts Estates](#trusts-estates) — 12 skills
- [Bankruptcy Restructuring](#bankruptcy-restructuring) — 12 skills
- [Insurance](#insurance) — 12 skills
- [Securities Capital Markets](#securities-capital-markets) — 12 skills
- [Real Estate](#real-estate) — 9 skills
- [Tax](#tax) — 10 skills
- [Financial Crime](#financial-crime) — 2 skills
- [Legal Ops](#legal-ops) — 3 skills

---

## Legal Research

### Legal Research Memo

- **Skill file:** `skills/legal-research/legal-research-memo/SKILL.md`
- **Practice area:** Legal Research
- **Current description:** Use when producing a structured legal research memo in response to a specific legal question, organizing analysis using IRAC discipline (Question Presented, Brief Answer, Facts, Assumptions, Discussion/Analysis, Conclusion) with explicit sourcing requirements and attorney verification checkpoints.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-research/legal-research-memo/SKILL.md
- Current skill name: Legal Research Memo
- Practice area: Legal Research
- Current description: Use when producing a structured legal research memo in response to a specific legal question, organizing analysis using IRAC discipline (Question Presented, Brief Answer, Facts, Assumptions, Discussion/Analysis, Conclusion) with explicit sourcing requirements and attorney verification checkpoints.
- Core purpose (must not change): Produce a structured, attorney-ready legal research memo in response to a discrete legal question.

CONTEXT TO READ FIRST
- Read skills/legal-research/legal-research-memo/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Litigation

### Brief Section Drafter

- **Skill file:** `skills/litigation/brief-section-drafter/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when drafting a single section of a litigation brief — such as a statement of facts, argument section, standard of review, or conclusion — that is cited to the record, consistent with the case theory, and fully flagged for attorney verification before filing.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/brief-section-drafter/SKILL.md
- Current skill name: Brief Section Drafter
- Practice area: Litigation
- Current description: Use when drafting a single section of a litigation brief — such as a statement of facts, argument section, standard of review, or conclusion — that is cited to the record, consistent with the case theory, and fully flagged for attorney verification before filing.
- Core purpose (must not change): Produce a first draft of a single section of a litigation brief for attorney review, revision, and sign-off.

CONTEXT TO READ FIRST
- Read skills/litigation/brief-section-drafter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Claim Chart

- **Skill file:** `skills/litigation/claim-chart/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building an element-by-element claim chart — mapping patent claim limitations or the elements of a civil cause of action or affirmative defense against evidence — to produce a structured gap analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/claim-chart/SKILL.md
- Current skill name: Claim Chart
- Practice area: Litigation
- Current description: Use when building an element-by-element claim chart — mapping patent claim limitations or the elements of a civil cause of action or affirmative defense against evidence — to produce a structured gap analysis for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready claim chart that maps discrete elements — either limitations of a patent claim or the required elements of a civil cause of action or affirmative defense — against the available evidence or accused features.

CONTEXT TO READ FIRST
- Read skills/litigation/claim-chart/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Demand Letter

- **Skill file:** `skills/litigation/demand-letter/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when drafting a demand letter for attorney review — running a structured intake, a pre-draft risk gate, and a draft with every legal conclusion, damages figure, and deadline flagged for attorney confirmation before sending.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/demand-letter/SKILL.md
- Current skill name: Demand Letter
- Practice area: Litigation
- Current description: Use when drafting a demand letter for attorney review — running a structured intake, a pre-draft risk gate, and a draft with every legal conclusion, damages figure, and deadline flagged for attorney confirmation before sending.
- Core purpose (must not change): Produce a structured draft demand letter for attorney review, revision, and sending.

CONTEXT TO READ FIRST
- Read skills/litigation/demand-letter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Deposition Preparation

- **Skill file:** `skills/litigation/deposition-prep/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building a structured deposition outline from the case theory, witness profile, and documentary record to produce a focused, attorney-ready map for taking or defending a deposition.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/deposition-prep/SKILL.md
- Current skill name: Deposition Preparation
- Practice area: Litigation
- Current description: Use when building a structured deposition outline from the case theory, witness profile, and documentary record to produce a focused, attorney-ready map for taking or defending a deposition.
- Core purpose (must not change): Produce a structured deposition outline for attorney review and use.

CONTEXT TO READ FIRST
- Read skills/litigation/deposition-prep/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Legal Hold

- **Skill file:** `skills/litigation/legal-hold/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when preparing a litigation hold notice and preservation-scope summary — issuing a new hold, refreshing an existing one, or releasing a closed one — as draft legal work product for attorney review before distribution to custodians.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/legal-hold/SKILL.md
- Current skill name: Legal Hold
- Practice area: Litigation
- Current description: Use when preparing a litigation hold notice and preservation-scope summary — issuing a new hold, refreshing an existing one, or releasing a closed one — as draft legal work product for attorney review before distribution to custodians.
- Core purpose (must not change): Produce a structured draft litigation hold notice and preservation-scope summary for attorney review.

CONTEXT TO READ FIRST
- Read skills/litigation/legal-hold/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Litigation Chronology

- **Skill file:** `skills/litigation/litigation-chronology/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building a factual timeline for litigation from provided source documents, producing a structured chronology table with citations, disputed/undisputed flags, and gap analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/litigation-chronology/SKILL.md
- Current skill name: Litigation Chronology
- Practice area: Litigation
- Current description: Use when building a factual timeline for litigation from provided source documents, producing a structured chronology table with citations, disputed/undisputed flags, and gap analysis for attorney review.
- Core purpose (must not change): Produce a structured, sourced factual chronology of events relevant to a litigation or dispute matter.

CONTEXT TO READ FIRST
- Read skills/litigation/litigation-chronology/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Matter Intake

- **Skill file:** `skills/litigation/matter-intake/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when opening a new litigation or dispute matter to produce a structured intake summary capturing parties, claims, jurisdiction, key dates, and immediate action flags for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/matter-intake/SKILL.md
- Current skill name: Matter Intake
- Practice area: Litigation
- Current description: Use when opening a new litigation or dispute matter to produce a structured intake summary capturing parties, claims, jurisdiction, key dates, and immediate action flags for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready intake summary for a new litigation or dispute matter.

CONTEXT TO READ FIRST
- Read skills/litigation/matter-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privilege Log Review

- **Skill file:** `skills/litigation/privilege-log-review/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when conducting a first-pass review of a privilege log to sort entries into three tiers — confidently privileged, uncertain (flagged for attorney decision), and recommend-remove — so attorney review time focuses on the entries that require judgment.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/privilege-log-review/SKILL.md
- Current skill name: Privilege Log Review
- Practice area: Litigation
- Current description: Use when conducting a first-pass review of a privilege log to sort entries into three tiers — confidently privileged, uncertain (flagged for attorney decision), and recommend-remove — so attorney review time focuses on the entries that require judgment.
- Core purpose (must not change): Produce a structured, attorney-ready first-pass review of a privilege log.

CONTEXT TO READ FIRST
- Read skills/litigation/privilege-log-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Subpoena Triage

- **Skill file:** `skills/litigation/subpoena-triage/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when an incoming subpoena has been received to produce a structured triage summary identifying the compliance deadline, scope, objections, privilege issues, preservation obligations, and internal notification requirements for immediate attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/subpoena-triage/SKILL.md
- Current skill name: Subpoena Triage
- Practice area: Litigation
- Current description: Use when an incoming subpoena has been received to produce a structured triage summary identifying the compliance deadline, scope, objections, privilege issues, preservation obligations, and internal notification requirements for immediate attorney review.
- Core purpose (must not change): Produce a structured triage summary for an incoming subpoena requiring prompt attorney evaluation.

CONTEXT TO READ FIRST
- Read skills/litigation/subpoena-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Contracts

### Contract Risk Review

- **Skill file:** `skills/contracts/contract-risk-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a general commercial contract — such as a master services agreement, vendor agreement, supplier contract, or professional services agreement — to produce a structured risk assessment and prioritized issue list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/contract-risk-review/SKILL.md
- Current skill name: Contract Risk Review
- Practice area: Contracts
- Current description: Use when reviewing a general commercial contract — such as a master services agreement, vendor agreement, supplier contract, or professional services agreement — to produce a structured risk assessment and prioritized issue list for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready risk review of a commercial contract.

CONTEXT TO READ FIRST
- Read skills/contracts/contract-risk-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### NDA Review

- **Skill file:** `skills/contracts/nda-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a non-disclosure or confidentiality agreement to produce a triage rating (route, flag, or stop), a structured risk summary, and prioritized redline points for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/nda-review/SKILL.md
- Current skill name: NDA Review
- Practice area: Contracts
- Current description: Use when reviewing a non-disclosure or confidentiality agreement to produce a triage rating (route, flag, or stop), a structured risk summary, and prioritized redline points for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a non-disclosure agreement (NDA) or confidentiality agreement.

CONTEXT TO READ FIRST
- Read skills/contracts/nda-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Redline Summary

- **Skill file:** `skills/contracts/redline-summary/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when summarizing the substantive changes between two versions of a contract, a set of tracked changes, or a base agreement plus a series of amendments — to produce a change-by-change table and narrative of the most material shifts for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/redline-summary/SKILL.md
- Current skill name: Redline Summary
- Practice area: Contracts
- Current description: Use when summarizing the substantive changes between two versions of a contract, a set of tracked changes, or a base agreement plus a series of amendments — to produce a change-by-change table and narrative of the most material shifts for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready summary of the substantive edits between two versions of a contract, or within a single document containing tracked changes, or across a base agreement and a series of amendments.

CONTEXT TO READ FIRST
- Read skills/contracts/redline-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### SOW Review

- **Skill file:** `skills/contracts/sow-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a statement of work or work order to assess scope clarity, deliverables, acceptance criteria, timeline, pricing, and — critically — consistency with the governing master agreement.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/sow-review/SKILL.md
- Current skill name: SOW Review
- Practice area: Contracts
- Current description: Use when reviewing a statement of work or work order to assess scope clarity, deliverables, acceptance criteria, timeline, pricing, and — critically — consistency with the governing master agreement.
- Core purpose (must not change): Produce a structured, attorney-ready review of a statement of work (SOW) or work order (WO).

CONTEXT TO READ FIRST
- Read skills/contracts/sow-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Vendor Agreement Status

- **Skill file:** `skills/contracts/vendor-agreement-status/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when assembling a consolidated status report of the agreements in place with a given vendor — what is executed, current, expired, or missing — with a gap analysis, upcoming dates, and surviving obligations for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/vendor-agreement-status/SKILL.md
- Current skill name: Vendor Agreement Status
- Practice area: Contracts
- Current description: Use when assembling a consolidated status report of the agreements in place with a given vendor — what is executed, current, expired, or missing — with a gap analysis, upcoming dates, and surviving obligations for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft status report of the agreements in place with a given vendor.

CONTEXT TO READ FIRST
- Read skills/contracts/vendor-agreement-status/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Corporate

### Board Minutes

- **Skill file:** `skills/corporate/board-minutes/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when drafting minutes of a board of directors or board-committee meeting as a corporate record, producing a structured draft for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/board-minutes/SKILL.md
- Current skill name: Board Minutes
- Practice area: Corporate
- Current description: Use when drafting minutes of a board of directors or board-committee meeting as a corporate record, producing a structured draft for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft of minutes for a meeting of a board of directors or a board committee.

CONTEXT TO READ FIRST
- Read skills/corporate/board-minutes/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Closing Checklist

- **Skill file:** `skills/corporate/closing-checklist/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when building or updating a transaction closing checklist — conditions precedent, closing deliverables, and pre-closing actions — and identifying what is blocking a deal closing, as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/closing-checklist/SKILL.md
- Current skill name: Closing Checklist
- Practice area: Corporate
- Current description: Use when building or updating a transaction closing checklist — conditions precedent, closing deliverables, and pre-closing actions — and identifying what is blocking a deal closing, as draft work product for attorney review.
- Core purpose (must not change): Build and maintain a structured transaction closing checklist that captures every condition precedent, closing deliverable, and pre-closing covenant drawn from the transaction agreement.

CONTEXT TO READ FIRST
- Read skills/corporate/closing-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Diligence Issue Extraction

- **Skill file:** `skills/corporate/diligence-issue-extraction/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when reviewing provided due-diligence documents for an M&A or investment transaction to extract material issues into a structured, severity-sorted issues memo for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/diligence-issue-extraction/SKILL.md
- Current skill name: Diligence Issue Extraction
- Practice area: Corporate
- Current description: Use when reviewing provided due-diligence documents for an M&A or investment transaction to extract material issues into a structured, severity-sorted issues memo for attorney review.
- Core purpose (must not change): Extract and organize material issues from a target's due-diligence documents into a structured issues memo, ready for attorney review.

CONTEXT TO READ FIRST
- Read skills/corporate/diligence-issue-extraction/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Entity Compliance Review

- **Skill file:** `skills/corporate/entity-compliance/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when organizing and reviewing an organization's corporate-entity compliance status — periodic filings, registered-agent status, and good standing — into a structured review that flags overdue, due-soon, and unknown items as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/entity-compliance/SKILL.md
- Current skill name: Entity Compliance Review
- Practice area: Corporate
- Current description: Use when organizing and reviewing an organization's corporate-entity compliance status — periodic filings, registered-agent status, and good standing — into a structured review that flags overdue, due-soon, and unknown items as draft work product for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an organization's corporate-entity compliance posture.

CONTEXT TO READ FIRST
- Read skills/corporate/entity-compliance/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Material Contract Schedule

- **Skill file:** `skills/corporate/material-contract-schedule/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when building the disclosure schedule that lists contracts meeting a transaction agreement's definition of "Material Contract," producing a draft for attorney review before it is delivered as an exhibit.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/material-contract-schedule/SKILL.md
- Current skill name: Material Contract Schedule
- Practice area: Corporate
- Current description: Use when building the disclosure schedule that lists contracts meeting a transaction agreement's definition of "Material Contract," producing a draft for attorney review before it is delivered as an exhibit.
- Core purpose (must not change): Produce a structured, attorney-ready draft of the Material Contract Schedule required as an exhibit to a transaction agreement.

CONTEXT TO READ FIRST
- Read skills/corporate/material-contract-schedule/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Written Consent

- **Skill file:** `skills/corporate/written-consent/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when drafting a board or board-committee action by written consent in lieu of a meeting to produce a structured draft for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/written-consent/SKILL.md
- Current skill name: Written Consent
- Practice area: Corporate
- Current description: Use when drafting a board or board-committee action by written consent in lieu of a meeting to produce a structured draft for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft of a corporate action taken by written consent in lieu of a meeting.

CONTEXT TO READ FIRST
- Read skills/corporate/written-consent/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Employment

### Employee Policy Review

- **Skill file:** `skills/employment/employee-policy-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing an employee handbook or individual workplace policy — or a proposed change to one — for clarity, internal consistency, completeness, and legal risk flags requiring attorney and jurisdiction-specific review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/employee-policy-review/SKILL.md
- Current skill name: Employee Policy Review
- Practice area: Employment
- Current description: Use when reviewing an employee handbook or individual workplace policy — or a proposed change to one — for clarity, internal consistency, completeness, and legal risk flags requiring attorney and jurisdiction-specific review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employee handbook or individual workplace policy, or of a proposed change to an existing handbook or policy.

CONTEXT TO READ FIRST
- Read skills/employment/employee-policy-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Hiring Review

- **Skill file:** `skills/employment/hiring-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing an employment offer letter (and any restrictive-covenant exhibits) against the candidate's actual work jurisdiction and the employer's standard hiring practices to produce a structured draft review memo with a verdict, for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/hiring-review/SKILL.md
- Current skill name: Hiring Review
- Practice area: Employment
- Current description: Use when reviewing an employment offer letter (and any restrictive-covenant exhibits) against the candidate's actual work jurisdiction and the employer's standard hiring practices to produce a structured draft review memo with a verdict, for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employment offer letter and any accompanying restrictive-covenant exhibits.

CONTEXT TO READ FIRST
- Read skills/employment/hiring-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Internal Investigation

- **Skill file:** `skills/employment/internal-investigation/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when supporting an attorney-directed internal investigation to track evidentiary coverage, connect evidence to issues, and draft a privileged investigation memorandum and audience-specific summaries as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/internal-investigation/SKILL.md
- Current skill name: Internal Investigation
- Practice area: Employment
- Current description: Use when supporting an attorney-directed internal investigation to track evidentiary coverage, connect evidence to issues, and draft a privileged investigation memorandum and audience-specific summaries as draft work product for attorney review.
- Core purpose (must not change): Provide structured workflow support for an attorney-directed internal investigation.

CONTEXT TO READ FIRST
- Read skills/employment/internal-investigation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Protected Leave Review

- **Skill file:** `skills/employment/protected-leave-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing and reviewing a protected-leave situation to identify the leave frameworks that may apply, surface procedural obligations and deadlines requiring verified research, and flag adverse-action exposure — producing draft legal work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/protected-leave-review/SKILL.md
- Current skill name: Protected Leave Review
- Practice area: Employment
- Current description: Use when organizing and reviewing a protected-leave situation to identify the leave frameworks that may apply, surface procedural obligations and deadlines requiring verified research, and flag adverse-action exposure — producing draft legal work product for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employee's protected-leave situation.

CONTEXT TO READ FIRST
- Read skills/employment/protected-leave-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Severance Review

- **Skill file:** `skills/employment/severance-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing a severance or separation agreement to produce a structured analysis of consideration, release scope, restrictive covenants, and open legal issues for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/severance-review/SKILL.md
- Current skill name: Severance Review
- Practice area: Employment
- Current description: Use when reviewing a severance or separation agreement to produce a structured analysis of consideration, release scope, restrictive covenants, and open legal issues for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a severance or separation agreement.

CONTEXT TO READ FIRST
- Read skills/employment/severance-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Termination Risk

- **Skill file:** `skills/employment/termination-risk/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing and reviewing the facts surrounding a proposed employee termination so an attorney can assess legal risk before the termination occurs.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/termination-risk/SKILL.md
- Current skill name: Termination Risk
- Practice area: Employment
- Current description: Use when organizing and reviewing the facts surrounding a proposed employee termination so an attorney can assess legal risk before the termination occurs.
- Core purpose (must not change): Produce a structured, attorney-ready fact-and-risk summary for a proposed employee termination.

CONTEXT TO READ FIRST
- Read skills/employment/termination-risk/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Wage and Hour Question Triage

- **Skill file:** `skills/employment/wage-hour-qa/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when a specific wage-and-hour or related employment question — such as overtime eligibility, exemption status, break entitlements, final-pay timing, paid-time-off payout, or leave eligibility — needs to be structured and analyzed against verified, jurisdiction-specific rules, producing a draft analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/wage-hour-qa/SKILL.md
- Current skill name: Wage and Hour Question Triage
- Practice area: Employment
- Current description: Use when a specific wage-and-hour or related employment question — such as overtime eligibility, exemption status, break entitlements, final-pay timing, paid-time-off payout, or leave eligibility — needs to be structured and analyzed against verified, jurisdiction-specific rules, producing a draft analysis for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft analysis of a specific wage-and-hour or related employment question.

CONTEXT TO READ FIRST
- Read skills/employment/wage-hour-qa/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Worker Classification

- **Skill file:** `skills/employment/worker-classification/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing the facts of a proposed worker engagement and structuring an analysis of whether the worker should be classified as an employee or an independent contractor, producing a draft classification analysis memo for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/worker-classification/SKILL.md
- Current skill name: Worker Classification
- Practice area: Employment
- Current description: Use when organizing the facts of a proposed worker engagement and structuring an analysis of whether the worker should be classified as an employee or an independent contractor, producing a draft classification analysis memo for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready classification analysis memo for a **proposed** worker engagement — one that has not yet started.

CONTEXT TO READ FIRST
- Read skills/employment/worker-classification/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Privacy

### DPA Review

- **Skill file:** `skills/privacy/dpa-review/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when reviewing a data processing agreement (DPA) or data processing addendum to produce a structured risk summary and prioritized issues for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/dpa-review/SKILL.md
- Current skill name: DPA Review
- Practice area: Privacy
- Current description: Use when reviewing a data processing agreement (DPA) or data processing addendum to produce a structured risk summary and prioritized issues for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a data processing agreement (DPA) or data processing addendum.

CONTEXT TO READ FIRST
- Read skills/privacy/dpa-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### DSAR Workflow

- **Skill file:** `skills/privacy/dsar-workflow/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when an organization receives a data subject access request (or any data subject rights request) and needs a structured triage, handling record, and response plan for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/dsar-workflow/SKILL.md
- Current skill name: DSAR Workflow
- Practice area: Privacy
- Current description: Use when an organization receives a data subject access request (or any data subject rights request) and needs a structured triage, handling record, and response plan for attorney review.
- Core purpose (must not change): Produce a structured triage record and response plan for a data subject access request (DSAR) or other data subject rights request (deletion, correction, portability, restriction, objection, opt-out, etc.).

CONTEXT TO READ FIRST
- Read skills/privacy/dsar-workflow/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Impact Assessment

- **Skill file:** `skills/privacy/pia-generation/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when drafting an internal privacy impact assessment for a new or changed processing activity to document the data involved, design-linked risks, policy consistency, and mitigations for privacy-counsel review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/pia-generation/SKILL.md
- Current skill name: Privacy Impact Assessment
- Practice area: Privacy
- Current description: Use when drafting an internal privacy impact assessment for a new or changed processing activity to document the data involved, design-linked risks, policy consistency, and mitigations for privacy-counsel review.
- Core purpose (must not change): Produce a structured, attorney-ready draft Privacy Impact Assessment (PIA) for a proposed or recently changed processing activity.

CONTEXT TO READ FIRST
- Read skills/privacy/pia-generation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Policy Gap Review

- **Skill file:** `skills/privacy/privacy-policy-gap-review/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when reviewing a published privacy policy or privacy notice to identify gaps, internal inconsistencies, and discrepancies between what the policy says and the organization's described actual data practices.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/privacy-policy-gap-review/SKILL.md
- Current skill name: Privacy Policy Gap Review
- Practice area: Privacy
- Current description: Use when reviewing a published privacy policy or privacy notice to identify gaps, internal inconsistencies, and discrepancies between what the policy says and the organization's described actual data practices.
- Core purpose (must not change): Produce a structured, attorney-ready gap review of a published privacy policy or privacy notice.

CONTEXT TO READ FIRST
- Read skills/privacy/privacy-policy-gap-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Product Legal

### AI Feature Review

- **Skill file:** `skills/product-legal/ai-feature-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when conducting a legal issue-spotting review for a product feature that uses AI or machine learning to produce a structured issues register and recommendations for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/ai-feature-review/SKILL.md
- Current skill name: AI Feature Review
- Practice area: Product Legal
- Current description: Use when conducting a legal issue-spotting review for a product feature that uses AI or machine learning to produce a structured issues register and recommendations for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready legal issues register for a product feature that uses artificial intelligence or machine learning.

CONTEXT TO READ FIRST
- Read skills/product-legal/ai-feature-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Launch Review

- **Skill file:** `skills/product-legal/launch-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when conducting a pre-launch legal issue-spotting review for a new product or feature to produce a structured issues register and a draft go/hold/conditions recommendation for attorney sign-off.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/launch-review/SKILL.md
- Current skill name: Launch Review
- Practice area: Product Legal
- Current description: Use when conducting a pre-launch legal issue-spotting review for a new product or feature to produce a structured issues register and a draft go/hold/conditions recommendation for attorney sign-off.
- Core purpose (must not change): Produce a structured, attorney-ready legal issues register for a product or feature before it launches.

CONTEXT TO READ FIRST
- Read skills/product-legal/launch-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Marketing Claims Review

- **Skill file:** `skills/product-legal/marketing-claims-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when reviewing marketing or advertising copy for legal risk to produce a claim-by-claim analysis, substantiation requirements, and flagged disclosures for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/marketing-claims-review/SKILL.md
- Current skill name: Marketing Claims Review
- Practice area: Product Legal
- Current description: Use when reviewing marketing or advertising copy for legal risk to produce a claim-by-claim analysis, substantiation requirements, and flagged disclosures for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of marketing or advertising copy.

CONTEXT TO READ FIRST
- Read skills/product-legal/marketing-claims-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Terms of Service Review

- **Skill file:** `skills/product-legal/terms-of-service-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when reviewing a terms of service or terms of use document to produce a structured risk summary, issues table, and prioritized recommendations for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/terms-of-service-review/SKILL.md
- Current skill name: Terms of Service Review
- Practice area: Product Legal
- Current description: Use when reviewing a terms of service or terms of use document to produce a structured risk summary, issues table, and prioritized recommendations for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a terms of service (ToS) or terms of use (ToU) document, for either a consumer (B2C) or business-to-business (B2B) context.

CONTEXT TO READ FIRST
- Read skills/product-legal/terms-of-service-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Regulatory

### Compliance Gap Matrix

- **Skill file:** `skills/regulatory/compliance-gap-matrix/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when mapping a set of regulatory or framework requirements against an organization's current controls to surface gaps, prioritize remediation, and produce an attorney-ready draft gap analysis.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/compliance-gap-matrix/SKILL.md
- Current skill name: Compliance Gap Matrix
- Practice area: Regulatory
- Current description: Use when mapping a set of regulatory or framework requirements against an organization's current controls to surface gaps, prioritize remediation, and produce an attorney-ready draft gap analysis.
- Core purpose (must not change): Produce a structured, attorney-ready draft compliance gap matrix that maps discrete regulatory or framework requirements against the organization's described current controls and practices.

CONTEXT TO READ FIRST
- Read skills/regulatory/compliance-gap-matrix/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Compliance Program Tracker

- **Skill file:** `skills/regulatory/compliance-program-tracker/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when building an ongoing compliance-program tracker for a framework — mapping requirements to controls, owners, and evidence, building an audit calendar, and surfacing evidence gaps and remediation items for attorney review and audit readiness.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/compliance-program-tracker/SKILL.md
- Current skill name: Compliance Program Tracker
- Practice area: Regulatory
- Current description: Use when building an ongoing compliance-program tracker for a framework — mapping requirements to controls, owners, and evidence, building an audit calendar, and surfacing evidence gaps and remediation items for attorney review and audit readiness.
- Core purpose (must not change): Produce a structured, attorney-ready draft compliance-program tracker for a regulatory framework or standard.

CONTEXT TO READ FIRST
- Read skills/regulatory/compliance-program-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Enforcement Risk Memo

- **Skill file:** `skills/regulatory/enforcement-risk-memo/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when structuring a memo that assesses potential enforcement exposure for a described practice, conduct, or set of facts, to produce attorney-ready draft analysis for review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/enforcement-risk-memo/SKILL.md
- Current skill name: Enforcement Risk Memo
- Practice area: Regulatory
- Current description: Use when structuring a memo that assesses potential enforcement exposure for a described practice, conduct, or set of facts, to produce attorney-ready draft analysis for review.
- Core purpose (must not change): Produce a structured, attorney-ready draft memo assessing potential regulatory enforcement exposure for a described situation, conduct, or practice.

CONTEXT TO READ FIRST
- Read skills/regulatory/enforcement-risk-memo/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Rule Change Summary

- **Skill file:** `skills/regulatory/rule-change-summary/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when summarizing a regulatory rule change, proposed rule, or official guidance document and its practical impact on an organization, based on the actual document provided.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/rule-change-summary/SKILL.md
- Current skill name: Rule Change Summary
- Practice area: Regulatory
- Current description: Use when summarizing a regulatory rule change, proposed rule, or official guidance document and its practical impact on an organization, based on the actual document provided.
- Core purpose (must not change): Produce a structured, attorney-ready summary of a regulatory rule change, proposed rule, interim final rule, or official guidance document, and identify its practical implications for a specific organization or practice area.

CONTEXT TO READ FIRST
- Read skills/regulatory/rule-change-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## AI Governance

### AI Use Case Intake

- **Skill file:** `skills/ai-governance/ai-use-case-intake/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when a new or modified AI/ML use case needs to be documented and triaged so legal, compliance, and governance teams can assess risk and route it to the right specialists.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/ai-use-case-intake/SKILL.md
- Current skill name: AI Use Case Intake
- Practice area: AI Governance
- Current description: Use when a new or modified AI/ML use case needs to be documented and triaged so legal, compliance, and governance teams can assess risk and route it to the right specialists.
- Core purpose (must not change): Produce a structured, attorney-ready intake record for a proposed or in-flight AI/ML use case.

CONTEXT TO READ FIRST
- Read skills/ai-governance/ai-use-case-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### AI Vendor Terms Review

- **Skill file:** `skills/ai-governance/ai-vendor-terms-review/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when reviewing the terms of service, API agreement, or usage policies of an AI vendor or AI-enabled service to produce a structured risk summary and prioritized redline points for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/ai-vendor-terms-review/SKILL.md
- Current skill name: AI Vendor Terms Review
- Practice area: AI Governance
- Current description: Use when reviewing the terms of service, API agreement, or usage policies of an AI vendor or AI-enabled service to produce a structured risk summary and prioritized redline points for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an AI vendor's terms of service, API agreement, enterprise license, or acceptable use policy.

CONTEXT TO READ FIRST
- Read skills/ai-governance/ai-vendor-terms-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Employee AI Policy

- **Skill file:** `skills/ai-governance/employee-ai-policy/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when reviewing a draft internal employee AI-use policy — or drafting review criteria when no policy exists — to identify gaps, inconsistencies, and priority issues for attorney and HR review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/employee-ai-policy/SKILL.md
- Current skill name: Employee AI Policy
- Practice area: AI Governance
- Current description: Use when reviewing a draft internal employee AI-use policy — or drafting review criteria when no policy exists — to identify gaps, inconsistencies, and priority issues for attorney and HR review.
- Core purpose (must not change): Produce a structured review of an organization's internal employee AI-use policy (or, if no policy exists, a structured gap analysis based on the topics a policy should address).

CONTEXT TO READ FIRST
- Read skills/ai-governance/employee-ai-policy/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Model Risk Triage

- **Skill file:** `skills/ai-governance/model-risk-triage/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when triaging the legal and governance risk of a specific AI model or AI system before or during deployment, to produce a structured risk register and recommended controls for attorney and governance review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/model-risk-triage/SKILL.md
- Current skill name: Model Risk Triage
- Practice area: AI Governance
- Current description: Use when triaging the legal and governance risk of a specific AI model or AI system before or during deployment, to produce a structured risk register and recommended controls for attorney and governance review.
- Core purpose (must not change): Produce a structured triage of the legal, governance, and operational risk dimensions of a specific AI model or AI system.

CONTEXT TO READ FIRST
- Read skills/ai-governance/model-risk-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Intellectual Property

### Cease and Desist Response

- **Skill file:** `skills/ip/cease-and-desist-response/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a client has received a cease-and-desist letter or similar IP demand and needs a structured triage memo — covering the assertion, exposure, response options, and immediate actions — for attorney review before responding.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/cease-and-desist-response/SKILL.md
- Current skill name: Cease and Desist Response
- Practice area: Intellectual Property
- Current description: Use when a client has received a cease-and-desist letter or similar IP demand and needs a structured triage memo — covering the assertion, exposure, response options, and immediate actions — for attorney review before responding.
- Core purpose (must not change): Produce a structured triage memo for a client who has received a cease-and-desist letter or similar intellectual property demand.

CONTEXT TO READ FIRST
- Read skills/ip/cease-and-desist-response/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### DMCA Takedown

- **Skill file:** `skills/ip/dmca-takedown/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when preparing a takedown notice for infringing content or evaluating a takedown notice received against the user's content, including whether a counter-notice may be appropriate.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/dmca-takedown/SKILL.md
- Current skill name: DMCA Takedown
- Practice area: Intellectual Property
- Current description: Use when preparing a takedown notice for infringing content or evaluating a takedown notice received against the user's content, including whether a counter-notice may be appropriate.
- Core purpose (must not change): Produce a structured draft notice or counter-notice under the Digital Millennium Copyright Act (DMCA) notice-and-takedown framework, or evaluate a notice received against the user's content.

CONTEXT TO READ FIRST
- Read skills/ip/dmca-takedown/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Infringement Triage

- **Skill file:** `skills/ip/infringement-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a client needs a first-pass, structured triage of a potential intellectual property infringement issue — identifying the key factors, flagging their direction, and routing to IP counsel — without concluding whether infringement occurred.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/infringement-triage/SKILL.md
- Current skill name: Infringement Triage
- Practice area: Intellectual Property
- Current description: Use when a client needs a first-pass, structured triage of a potential intellectual property infringement issue — identifying the key factors, flagging their direction, and routing to IP counsel — without concluding whether infringement occurred.
- Core purpose (must not change): Produce a structured first-pass triage memo for a potential intellectual property infringement issue.

CONTEXT TO READ FIRST
- Read skills/ip/infringement-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Invention Disclosure Intake

- **Skill file:** `skills/ip/invention-intake/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user wants a structured first-pass screen of a proposed invention to assess whether it warrants patent-counsel attention, with filing-deadline urgency flagging.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/invention-intake/SKILL.md
- Current skill name: Invention Disclosure Intake
- Practice area: Intellectual Property
- Current description: Use when a user wants a structured first-pass screen of a proposed invention to assess whether it warrants patent-counsel attention, with filing-deadline urgency flagging.
- Core purpose (must not change): Produce a structured first-pass invention disclosure screen that helps patent counsel quickly orient to a proposed invention, identify obvious patentability signals, and flag any urgent filing-deadline concerns.

CONTEXT TO READ FIRST
- Read skills/ip/invention-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Open Source License Review

- **Skill file:** `skills/ip/open-source-license-review/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when reviewing the open-source licenses present in or proposed for a project to identify compliance obligations and flag compatibility or disclosure risks for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/open-source-license-review/SKILL.md
- Current skill name: Open Source License Review
- Practice area: Intellectual Property
- Current description: Use when reviewing the open-source licenses present in or proposed for a project to identify compliance obligations and flag compatibility or disclosure risks for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of the open-source licenses present in a software project or proposed for adoption.

CONTEXT TO READ FIRST
- Read skills/ip/open-source-license-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Patent Freedom-to-Operate Triage

- **Skill file:** `skills/ip/fto-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user needs a structured first-pass review of potential patent blocking issues for a product, process, or feature against a set of identified patents, to flag risk areas and route findings to patent counsel for a formal freedom-to-operate opinion.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/fto-triage/SKILL.md
- Current skill name: Patent Freedom-to-Operate Triage
- Practice area: Intellectual Property
- Current description: Use when a user needs a structured first-pass review of potential patent blocking issues for a product, process, or feature against a set of identified patents, to flag risk areas and route findings to patent counsel for a formal freedom-to-operate opinion.
- Core purpose (must not change): Produce a structured, attorney-ready triage memo that identifies potential patent blocking issues for a described product, process, or feature against a specific set of patents the user has already identified.

CONTEXT TO READ FIRST
- Read skills/ip/fto-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Trademark Clearance Triage

- **Skill file:** `skills/ip/trademark-clearance-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user wants a preliminary triage of a proposed brand name, product name, or logo before engaging trademark counsel for a formal clearance search.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/trademark-clearance-triage/SKILL.md
- Current skill name: Trademark Clearance Triage
- Practice area: Intellectual Property
- Current description: Use when a user wants a preliminary triage of a proposed brand name, product name, or logo before engaging trademark counsel for a formal clearance search.
- Core purpose (must not change): Produce a structured preliminary triage of a proposed trademark — a brand name, product name, slogan, or logo — to identify obvious concerns and orient the client before engaging trademark counsel for a formal clearance search and opinion.

CONTEXT TO READ FIRST
- Read skills/ip/trademark-clearance-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Setup

### Contracts Cold-Start Interview

- **Skill file:** `skills/setup/contracts-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a contracts practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/contracts-cold-start-interview/SKILL.md
- Current skill name: Contracts Cold-Start Interview
- Practice area: Setup
- Current description: Use when a contracts practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a contracts practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/contracts.md`.

CONTEXT TO READ FIRST
- Read skills/setup/contracts-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Corporate Cold-Start Interview

- **Skill file:** `skills/setup/corporate-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a corporate practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/corporate-cold-start-interview/SKILL.md
- Current skill name: Corporate Cold-Start Interview
- Practice area: Setup
- Current description: Use when a corporate practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a corporate practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/corporate.md`.

CONTEXT TO READ FIRST
- Read skills/setup/corporate-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Create Matter Workspace

- **Skill file:** `skills/setup/create-matter-workspace/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when starting a new legal matter that will span multiple skills, documents, or deadlines, to select the right matter-workspace template, gather core matter information, and produce a populated workspace draft for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/create-matter-workspace/SKILL.md
- Current skill name: Create Matter Workspace
- Practice area: Setup
- Current description: Use when starting a new legal matter that will span multiple skills, documents, or deadlines, to select the right matter-workspace template, gather core matter information, and produce a populated workspace draft for attorney review.
- Core purpose (must not change): Produce a populated matter-workspace draft — the single organizing file that carries one legal matter's parties, facts, jurisdiction, deadlines, documents, open items, and the draft work product produced by every skill run for that matter.

CONTEXT TO READ FIRST
- Read skills/setup/create-matter-workspace/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Litigation Cold-Start Interview

- **Skill file:** `skills/setup/litigation-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a litigation practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/litigation-cold-start-interview/SKILL.md
- Current skill name: Litigation Cold-Start Interview
- Practice area: Setup
- Current description: Use when a litigation practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a litigation practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/litigation.md`.

CONTEXT TO READ FIRST
- Read skills/setup/litigation-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Cold-Start Interview

- **Skill file:** `skills/setup/privacy-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a privacy practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/privacy-cold-start-interview/SKILL.md
- Current skill name: Privacy Cold-Start Interview
- Practice area: Setup
- Current description: Use when a privacy practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a privacy practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/privacy.md`.

CONTEXT TO READ FIRST
- Read skills/setup/privacy-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Legal Methodology

### Red-Team Verifier

- **Skill file:** `skills/legal-methodology/red-team-verifier/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when checking whether a legal memo, contract review, demand letter, risk matrix, research summary, draft filing, or other legal AI output is reliable enough to rely on — adversarially stress-testing it before attorney review to surface invented authority, unsupported claims, weak reasoning, jurisdiction and deadline gaps, and professional-responsibility issues.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/red-team-verifier/SKILL.md
- Current skill name: Red-Team Verifier
- Practice area: Legal Methodology
- Current description: Use when checking whether a legal memo, contract review, demand letter, risk matrix, research summary, draft filing, or other legal AI output is reliable enough to rely on — adversarially stress-testing it before attorney review to surface invented authority, unsupported claims, weak reasoning, jurisdiction and deadline gaps, and professional-responsibility issues.
- Core purpose (must not change): Adversarially stress-test a legal work product before anyone relies on it.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/red-team-verifier/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Risk Assessment

- **Skill file:** `skills/legal-methodology/risk-assessment/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when applying a structured, jurisdiction-agnostic method to identify, rate, and prioritize legal risks in a situation, producing a risk register with severity bands and a prioritized summary for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/risk-assessment/SKILL.md
- Current skill name: Risk Assessment
- Practice area: Legal Methodology
- Current description: Use when applying a structured, jurisdiction-agnostic method to identify, rate, and prioritize legal risks in a situation, producing a risk register with severity bands and a prioritized summary for attorney review.
- Core purpose (must not change): Apply a structured, jurisdiction-agnostic method for identifying, rating, and prioritizing legal risk across a situation, transaction, document, or matter.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/risk-assessment/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Source Validation

- **Skill file:** `skills/legal-methodology/source-validation/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed, applying the source hierarchy in `core/source-and-citation-discipline.md`.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/source-validation/SKILL.md
- Current skill name: Source Validation
- Practice area: Legal Methodology
- Current description: Use when checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed, applying the source hierarchy in `core/source-and-citation-discipline.md`.
- Core purpose (must not change): Apply a systematic method for checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/source-validation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Statutory Interpretation

- **Skill file:** `skills/legal-methodology/statutory-interpretation/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when applying a disciplined analytical method to parse and interpret a statutory, regulatory, or contractual provision — mapping structure, defined terms, and competing readings — to produce a structured interpretation worksheet for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/statutory-interpretation/SKILL.md
- Current skill name: Statutory Interpretation
- Practice area: Legal Methodology
- Current description: Use when applying a disciplined analytical method to parse and interpret a statutory, regulatory, or contractual provision — mapping structure, defined terms, and competing readings — to produce a structured interpretation worksheet for attorney review.
- Core purpose (must not change): Apply a disciplined, jurisdiction-agnostic analytical method for working through a statutory, regulatory, or contractual provision.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/statutory-interpretation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## M And A

### Acquisition Diligence Request List

- **Skill file:** `skills/m-and-a/acquisition-diligence-request-list/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when generating a tailored M&A due-diligence request list, organized by workstream, for a buyer or seller in an acquisition.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/acquisition-diligence-request-list/SKILL.md
- Current skill name: Acquisition Diligence Request List
- Practice area: M And A
- Current description: Use when generating a tailored M&A due-diligence request list, organized by workstream, for a buyer or seller in an acquisition.
- Core purpose (must not change): Generate a tailored due-diligence request list for a merger, acquisition, or strategic investment — the list of documents, data, and information a buyer asks the target to produce, or that a seller prepares to populate a data room.

CONTEXT TO READ FIRST
- Read skills/m-and-a/acquisition-diligence-request-list/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Data Room Index Review

- **Skill file:** `skills/m-and-a/data-room-index-review/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when reviewing an M&A data room index or file list to identify missing diligence categories, coverage gaps, and follow-up requests for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/data-room-index-review/SKILL.md
- Current skill name: Data Room Index Review
- Practice area: M And A
- Current description: Use when reviewing an M&A data room index or file list to identify missing diligence categories, coverage gaps, and follow-up requests for attorney review.
- Core purpose (must not change): Review a merger or acquisition data room index — or an uploaded file list — and identify, from a stated side of the deal, which diligence categories the index shows as covered, which appear partial or absent, and what follow-up requests the index suggests.

CONTEXT TO READ FIRST
- Read skills/m-and-a/data-room-index-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Indemnity and Escrow Risk Review

- **Skill file:** `skills/m-and-a/indemnity-escrow-risk-review/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when analyzing the indemnification and escrow architecture of an M&A purchase agreement — survival, caps, baskets, escrow, and recovery mechanics — for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/indemnity-escrow-risk-review/SKILL.md
- Current skill name: Indemnity and Escrow Risk Review
- Practice area: M And A
- Current description: Use when analyzing the indemnification and escrow architecture of an M&A purchase agreement — survival, caps, baskets, escrow, and recovery mechanics — for attorney review.
- Core purpose (must not change): Analyze the indemnification and escrow architecture of an M&A purchase agreement — a stock purchase agreement, asset purchase agreement, membership-interest purchase agreement, or merger agreement — and surface, from a stated side of the deal, how the agreement allocates post-closing risk: what survives and for how long, what limits and thresholds apply, how an indemnification claim is recovere...

CONTEXT TO READ FIRST
- Read skills/m-and-a/indemnity-escrow-risk-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Integration Legal Issues Checklist

- **Skill file:** `skills/m-and-a/integration-legal-issues-checklist/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when generating a legal integration checklist after signing or closing an M&A transaction, organized by workstream for legal and business owners.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/integration-legal-issues-checklist/SKILL.md
- Current skill name: Integration Legal Issues Checklist
- Practice area: M And A
- Current description: Use when generating a legal integration checklist after signing or closing an M&A transaction, organized by workstream for legal and business owners.
- Core purpose (must not change): Generate a structured legal integration checklist for a merger or acquisition after signing or closing, organized by workstream so legal and business owners can see, in one place, the integration tasks that carry a legal dimension, who owns each, and which items must be escalated.

CONTEXT TO READ FIRST
- Read skills/m-and-a/integration-legal-issues-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### LOI and Term Sheet Review

- **Skill file:** `skills/m-and-a/loi-term-sheet-review/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when reviewing a letter of intent, term sheet, or indication of interest for an M&A transaction to surface the deal terms, the binding-versus-non-binding provisions, and negotiation issues for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/loi-term-sheet-review/SKILL.md
- Current skill name: LOI and Term Sheet Review
- Practice area: M And A
- Current description: Use when reviewing a letter of intent, term sheet, or indication of interest for an M&A transaction to surface the deal terms, the binding-versus-non-binding provisions, and negotiation issues for attorney review.
- Core purpose (must not change): Review a letter of intent (LOI), term sheet, or indication of interest (IOI) for a merger, acquisition, or strategic investment, and surface — from a stated side of the deal — the deal terms it sets, how it characterizes each provision as binding or non-binding, the issues worth negotiating, and the terms it leaves open.

CONTEXT TO READ FIRST
- Read skills/m-and-a/loi-term-sheet-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### M&A Closing Deliverables Tracker

- **Skill file:** `skills/m-and-a/closing-deliverables-tracker/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when building a closing checklist of deliverables for an M&A transaction, tracking responsible party, status, dependencies, and signature and delivery needs.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/closing-deliverables-tracker/SKILL.md
- Current skill name: M&A Closing Deliverables Tracker
- Practice area: M And A
- Current description: Use when building a closing checklist of deliverables for an M&A transaction, tracking responsible party, status, dependencies, and signature and delivery needs.
- Core purpose (must not change): Build a closing-deliverables tracker for a merger, acquisition, or strategic investment: a structured checklist of the documents, certificates, consents, and other items the parties need to assemble, sign, and deliver to close the deal.

CONTEXT TO READ FIRST
- Read skills/m-and-a/closing-deliverables-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Post-Closing Obligations Tracker

- **Skill file:** `skills/m-and-a/post-closing-obligations-tracker/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when extracting and organizing the post-closing covenants and obligations from an M&A acquisition agreement and its ancillary documents into a tracked, source-cited obligation list.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/post-closing-obligations-tracker/SKILL.md
- Current skill name: Post-Closing Obligations Tracker
- Practice area: M And A
- Current description: Use when extracting and organizing the post-closing covenants and obligations from an M&A acquisition agreement and its ancillary documents into a tracked, source-cited obligation list.
- Core purpose (must not change): Extract the post-closing covenants and obligations from an executed or near-final M&A acquisition agreement and its ancillary documents, and organize them — from a stated side of the deal — into a single tracked obligation list, with a source citation for every obligation and a flag on anything the documents leave unstated.

CONTEXT TO READ FIRST
- Read skills/m-and-a/post-closing-obligations-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Purchase Agreement Issue List

- **Skill file:** `skills/m-and-a/purchase-agreement-issue-list/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when reviewing an M&A purchase agreement — a merger, stock purchase, asset purchase, or membership-interest purchase agreement — from a buyer or seller perspective to produce an issue list and risk matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/purchase-agreement-issue-list/SKILL.md
- Current skill name: Purchase Agreement Issue List
- Practice area: M And A
- Current description: Use when reviewing an M&A purchase agreement — a merger, stock purchase, asset purchase, or membership-interest purchase agreement — from a buyer or seller perspective to produce an issue list and risk matrix for attorney review.
- Core purpose (must not change): Review a definitive M&A acquisition agreement — a merger agreement, stock purchase agreement, asset purchase agreement, membership-interest purchase agreement, or a similar acquisition agreement — from a stated side of the deal, and surface the issues it raises: the deal structure and consideration it sets, the risk it allocates, the terms worth negotiating, and the provisions it leaves out.

CONTEXT TO READ FIRST
- Read skills/m-and-a/purchase-agreement-issue-list/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Reps and Warranties Disclosure Schedule Review

- **Skill file:** `skills/m-and-a/reps-warranties-disclosure-schedule-review/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when comparing the representations and warranties in an M&A purchase agreement against the disclosure schedules to surface gaps, mismatches, and unresolved items for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/reps-warranties-disclosure-schedule-review/SKILL.md
- Current skill name: Reps and Warranties Disclosure Schedule Review
- Practice area: M And A
- Current description: Use when comparing the representations and warranties in an M&A purchase agreement against the disclosure schedules to surface gaps, mismatches, and unresolved items for attorney review.
- Core purpose (must not change): Compare the representations and warranties in an M&A purchase agreement against the disclosure schedules that qualify them, and surface — from a stated side of the deal — where a representation has no matching schedule, where a schedule reference points nowhere, where an exception is drawn too broadly, where a defined term is used inconsistently, where a date has gone stale, and where the sched...

CONTEXT TO READ FIRST
- Read skills/m-and-a/reps-warranties-disclosure-schedule-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Third-Party Consents and Assignment Review

- **Skill file:** `skills/m-and-a/third-party-consents-assignment-review/SKILL.md`
- **Practice area:** M And A
- **Current description:** Use when identifying contractual consent, notice, change-of-control, and anti-assignment issues from M&A diligence contracts or summaries, organized into a consent tracker for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/m-and-a/third-party-consents-assignment-review/SKILL.md
- Current skill name: Third-Party Consents and Assignment Review
- Practice area: M And A
- Current description: Use when identifying contractual consent, notice, change-of-control, and anti-assignment issues from M&A diligence contracts or summaries, organized into a consent tracker for attorney review.
- Core purpose (must not change): Review the contracts or diligence summaries in an M&A matter and surface the clauses that a sale, merger, or assignment may trigger — consent, notice, change-of-control, anti-assignment, termination, and related provisions — then organize them into a single consent tracker the deal team can work from.

CONTEXT TO READ FIRST
- Read skills/m-and-a/third-party-consents-assignment-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Antitrust Competition

### Antitrust Compliance Policy Review

- **Skill file:** `skills/antitrust-competition/antitrust-compliance-policy-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing antitrust compliance policy review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/antitrust-compliance-policy-review/SKILL.md
- Current skill name: Antitrust Compliance Policy Review
- Practice area: Antitrust Competition
- Current description: Use when performing antitrust compliance policy review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for antitrust compliance policy review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/antitrust-compliance-policy-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Antitrust Risk Intake

- **Skill file:** `skills/antitrust-competition/antitrust-risk-intake/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing antitrust risk intake as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/antitrust-risk-intake/SKILL.md
- Current skill name: Antitrust Risk Intake
- Practice area: Antitrust Competition
- Current description: Use when performing antitrust risk intake as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for antitrust risk intake.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/antitrust-risk-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Competitor Collaboration Review

- **Skill file:** `skills/antitrust-competition/competitor-collaboration-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing competitor collaboration review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/competitor-collaboration-review/SKILL.md
- Current skill name: Competitor Collaboration Review
- Practice area: Antitrust Competition
- Current description: Use when performing competitor collaboration review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for competitor collaboration review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/competitor-collaboration-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Distribution Restraints Review

- **Skill file:** `skills/antitrust-competition/distribution-restraints-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing distribution restraints review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/distribution-restraints-review/SKILL.md
- Current skill name: Distribution Restraints Review
- Practice area: Antitrust Competition
- Current description: Use when performing distribution restraints review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for distribution restraints review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/distribution-restraints-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Exclusivity MFN Pricing Review

- **Skill file:** `skills/antitrust-competition/exclusivity-mfn-pricing-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing exclusivity mfn pricing review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/exclusivity-mfn-pricing-review/SKILL.md
- Current skill name: Exclusivity MFN Pricing Review
- Practice area: Antitrust Competition
- Current description: Use when performing exclusivity mfn pricing review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for exclusivity mfn pricing review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/exclusivity-mfn-pricing-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Gun Jumping Clean Team Checklist

- **Skill file:** `skills/antitrust-competition/gun-jumping-clean-team-checklist/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing gun jumping clean team checklist as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/gun-jumping-clean-team-checklist/SKILL.md
- Current skill name: Gun Jumping Clean Team Checklist
- Practice area: Antitrust Competition
- Current description: Use when performing gun jumping clean team checklist as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for gun jumping clean team checklist.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/gun-jumping-clean-team-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Information Sharing Clean Team Review

- **Skill file:** `skills/antitrust-competition/information-sharing-clean-team-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing information sharing clean team review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/information-sharing-clean-team-review/SKILL.md
- Current skill name: Information Sharing Clean Team Review
- Practice area: Antitrust Competition
- Current description: Use when performing information sharing clean team review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for information sharing clean team review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/information-sharing-clean-team-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Merger Antitrust Issue Spotter

- **Skill file:** `skills/antitrust-competition/merger-antitrust-issue-spotter/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing merger antitrust issue spotter as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/merger-antitrust-issue-spotter/SKILL.md
- Current skill name: Merger Antitrust Issue Spotter
- Practice area: Antitrust Competition
- Current description: Use when performing merger antitrust issue spotter as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for merger antitrust issue spotter.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/merger-antitrust-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Pricing Algorithm Risk Triage

- **Skill file:** `skills/antitrust-competition/pricing-algorithm-risk-triage/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing pricing algorithm risk triage as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/pricing-algorithm-risk-triage/SKILL.md
- Current skill name: Pricing Algorithm Risk Triage
- Practice area: Antitrust Competition
- Current description: Use when performing pricing algorithm risk triage as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for pricing algorithm risk triage.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/pricing-algorithm-risk-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Trade Association Meeting Review

- **Skill file:** `skills/antitrust-competition/trade-association-meeting-review/SKILL.md`
- **Practice area:** Antitrust Competition
- **Current description:** Use when performing trade association meeting review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/antitrust-competition/trade-association-meeting-review/SKILL.md
- Current skill name: Trade Association Meeting Review
- Practice area: Antitrust Competition
- Current description: Use when performing trade association meeting review as draft work product for attorney review.
- Core purpose (must not change): Produce a structured **draft for attorney review** for trade association meeting review.

CONTEXT TO READ FIRST
- Read skills/antitrust-competition/trade-association-meeting-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Family Law

### Asset / Debt Schedule Builder

- **Skill file:** `skills/family-law/asset-debt-schedule-builder/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when building a source-cited property and debt schedule from user-provided facts and documents for a family law matter, for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/asset-debt-schedule-builder/SKILL.md
- Current skill name: Asset / Debt Schedule Builder
- Practice area: Family Law
- Current description: Use when building a source-cited property and debt schedule from user-provided facts and documents for a family law matter, for attorney review.
- Core purpose (must not change): Build a source-cited property and debt schedule from user-provided facts and documents — real estate, bank accounts, investments, retirement accounts, business interests, vehicles, personal property, debts, credit cards, loans, and tax liabilities — so a qualified, licensed attorney has an organized schedule for a family law matter.

CONTEXT TO READ FIRST
- Read skills/family-law/asset-debt-schedule-builder/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Child Support Facts Intake

- **Skill file:** `skills/family-law/child-support-facts-intake/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when gathering the facts relevant to a child support review into a structured intake table, missing-documents list, and questions for counsel — without calculating support.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/child-support-facts-intake/SKILL.md
- Current skill name: Child Support Facts Intake
- Practice area: Family Law
- Current description: Use when gathering the facts relevant to a child support review into a structured intake table, missing-documents list, and questions for counsel — without calculating support.
- Core purpose (must not change): Gather the facts relevant to a child support review — the children, custody and parenting-time facts, income facts supplied by the user, healthcare and childcare costs, special expenses, existing orders, any stated arrears, and financial disclosures — into a structured intake table, a missing-documents list, and questions for counsel, so a qualified, licensed attorney has an organized fact set.

CONTEXT TO READ FIRST
- Read skills/family-law/child-support-facts-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Custody / Parenting Facts Chronology

- **Skill file:** `skills/family-law/custody-parenting-facts-chronology/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when building a source-cited chronology of parenting and caregiving facts for a custody or parenting dispute, for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/custody-parenting-facts-chronology/SKILL.md
- Current skill name: Custody / Parenting Facts Chronology
- Practice area: Family Law
- Current description: Use when building a source-cited chronology of parenting and caregiving facts for a custody or parenting dispute, for attorney review.
- Core purpose (must not change): Build a source-cited chronology of parenting and caregiving facts — caregiving roles, exchanges, school and medical involvement, communications, incidents, missed visits, relocation facts, and child-related records — so a qualified, licensed attorney has an organized factual timeline for a custody or parenting dispute.

CONTEXT TO READ FIRST
- Read skills/family-law/custody-parenting-facts-chronology/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Custody Order Review Checklist

- **Skill file:** `skills/family-law/custody-order-review-checklist/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when reviewing an existing or proposed custody or parenting order for clarity and administration issues, producing an order-clarity checklist and ambiguity table for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/custody-order-review-checklist/SKILL.md
- Current skill name: Custody Order Review Checklist
- Practice area: Family Law
- Current description: Use when reviewing an existing or proposed custody or parenting order for clarity and administration issues, producing an order-clarity checklist and ambiguity table for attorney review.
- Core purpose (must not change): Review an existing or proposed custody or parenting order for clarity and day-to-day administration issues — legal and physical custody language, the parenting schedule, holidays, exchanges, travel, communication, school and medical decisions, relocation and travel notices, dispute resolution, enforcement and ambiguity points, and any safety provisions — and produce an order-clarity checklist,...

CONTEXT TO READ FIRST
- Read skills/family-law/custody-order-review-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Divorce Intake Organizer

- **Skill file:** `skills/family-law/divorce-intake-organizer/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when organizing the facts of a divorce or dissolution matter into a structured facts table, missing-documents list, and issue map for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/divorce-intake-organizer/SKILL.md
- Current skill name: Divorce Intake Organizer
- Practice area: Family Law
- Current description: Use when organizing the facts of a divorce or dissolution matter into a structured facts table, missing-documents list, and issue map for attorney review.
- Core purpose (must not change): Organize the facts of a divorce or dissolution matter — dates, children, income, assets, debts, business and real-estate interests, retirement accounts, support and custody issues, existing agreements, and the contested or uncontested posture — into a structured facts table, a missing-documents list, and an issue map, so a qualified, licensed attorney has an organized factual foundation.

CONTEXT TO READ FIRST
- Read skills/family-law/divorce-intake-organizer/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Domestic Violence Safety Referral Checklist

- **Skill file:** `skills/family-law/domestic-violence-safety-referral-checklist/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when organizing facts and referral/escalation considerations for a domestic violence or abuse-related concern in a family law matter, for attorney review — without creating a safety plan.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/domestic-violence-safety-referral-checklist/SKILL.md
- Current skill name: Domestic Violence Safety Referral Checklist
- Practice area: Family Law
- Current description: Use when organizing facts and referral/escalation considerations for a domestic violence or abuse-related concern in a family law matter, for attorney review — without creating a safety plan.
- Core purpose (must not change): Organize the facts and the referral and escalation considerations for a domestic violence or abuse-related concern in a family law matter — immediate safety concerns, existing protective orders, described incidents, available records, children involved, housing and financial access concerns, and upcoming court dates — so a qualified, licensed attorney and the appropriate support resources can b...

CONTEXT TO READ FIRST
- Read skills/family-law/domestic-violence-safety-referral-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Family Law Discovery Tracker

- **Skill file:** `skills/family-law/family-law-discovery-tracker/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when organizing family-law discovery requests, responses, disclosures, subpoenas, authorizations, deficiencies, and meet-and-confer items into a tracker for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/family-law-discovery-tracker/SKILL.md
- Current skill name: Family Law Discovery Tracker
- Practice area: Family Law
- Current description: Use when organizing family-law discovery requests, responses, disclosures, subpoenas, authorizations, deficiencies, and meet-and-confer items into a tracker for attorney review.
- Core purpose (must not change): Organize family-law discovery — requests, responses, financial disclosures, subpoenas, authorizations, identified deficiencies, and meet-and-confer items — into a single tracker, so a qualified, licensed attorney can manage discovery in a family law matter.

CONTEXT TO READ FIRST
- Read skills/family-law/family-law-discovery-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Family Law Hearing Prep Checklist

- **Skill file:** `skills/family-law/family-law-hearing-prep-checklist/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when building a hearing-preparation checklist for a family law hearing — pleadings, declarations, exhibits, disclosures, witnesses, and open questions — for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/family-law-hearing-prep-checklist/SKILL.md
- Current skill name: Family Law Hearing Prep Checklist
- Practice area: Family Law
- Current description: Use when building a hearing-preparation checklist for a family law hearing — pleadings, declarations, exhibits, disclosures, witnesses, and open questions — for attorney review.
- Core purpose (must not change): Build a hearing-preparation checklist for a family law hearing — pleadings, declarations, exhibits, financial disclosures, witnesses, prior orders, service and proof documents, the issues to be heard, and the requested relief as the user supplies it — so a qualified, licensed attorney has an organized preparation list.

CONTEXT TO READ FIRST
- Read skills/family-law/family-law-hearing-prep-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Family Law Matter Intake

- **Skill file:** `skills/family-law/family-law-matter-intake/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when opening a new family law matter and you need a structured intake summary, issue map, missing-facts list, document request list, and safety/escalation flags for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/family-law-matter-intake/SKILL.md
- Current skill name: Family Law Matter Intake
- Practice area: Family Law
- Current description: Use when opening a new family law matter and you need a structured intake summary, issue map, missing-facts list, document request list, and safety/escalation flags for attorney review.
- Core purpose (must not change): Capture the facts of a new family law matter — divorce or dissolution, custody and parenting, support, property and debt, or a related dispute — into a structured intake summary, an issue map, a missing-facts list, a document request list, and safety/escalation flags, so a qualified, licensed attorney has an organized starting point.

CONTEXT TO READ FIRST
- Read skills/family-law/family-law-matter-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Parenting Schedule Facts Organizer

- **Skill file:** `skills/family-law/parenting-schedule-facts-organizer/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when organizing the facts relevant to a parenting schedule discussion into a facts table, conflict/ambiguity list, and logistics checklist for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/parenting-schedule-facts-organizer/SKILL.md
- Current skill name: Parenting Schedule Facts Organizer
- Practice area: Family Law
- Current description: Use when organizing the facts relevant to a parenting schedule discussion into a facts table, conflict/ambiguity list, and logistics checklist for attorney review.
- Core purpose (must not change): Organize the facts relevant to a parenting schedule discussion — the current and any proposed schedule, school and daycare, transportation, holidays and vacations, distance, work schedules, stated child needs, communication method, exchanges, and existing orders — into a facts table, a conflict/ambiguity list, and a logistics checklist, so a qualified, licensed attorney has an organized basis f...

CONTEXT TO READ FIRST
- Read skills/family-law/parenting-schedule-facts-organizer/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Settlement Agreement Issue Spotter

- **Skill file:** `skills/family-law/settlement-agreement-issue-spotter/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when reviewing a marital settlement, parenting, support, or custody-stipulation agreement to produce a key-terms table, issue list, ambiguity list, and missing-provisions list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/settlement-agreement-issue-spotter/SKILL.md
- Current skill name: Settlement Agreement Issue Spotter
- Practice area: Family Law
- Current description: Use when reviewing a marital settlement, parenting, support, or custody-stipulation agreement to produce a key-terms table, issue list, ambiguity list, and missing-provisions list for attorney review.
- Core purpose (must not change): Review a marital settlement agreement, parenting agreement, support agreement, custody stipulation, or related settlement draft and produce a key-terms table, an issue list, an ambiguity list, and a missing-provisions list, so a qualified, licensed attorney has an organized review.

CONTEXT TO READ FIRST
- Read skills/family-law/settlement-agreement-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Spousal Support Facts Intake

- **Skill file:** `skills/family-law/spousal-support-facts-intake/SKILL.md`
- **Practice area:** Family Law
- **Current description:** Use when gathering the facts relevant to a spousal support or alimony review into a structured intake table, missing-documents list, and questions for counsel — without calculating support.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/family-law/spousal-support-facts-intake/SKILL.md
- Current skill name: Spousal Support Facts Intake
- Practice area: Family Law
- Current description: Use when gathering the facts relevant to a spousal support or alimony review into a structured intake table, missing-documents list, and questions for counsel — without calculating support.
- Core purpose (must not change): Gather the facts relevant to a spousal support or alimony review — the relationship or marriage duration if provided, income, expenses, employment, health facts if provided, standard-of-living facts if provided, assets and debts, existing orders, and financial disclosures — into a structured intake table, a missing-documents list, and questions for counsel, so a qualified, licensed attorney has...

CONTEXT TO READ FIRST
- Read skills/family-law/spousal-support-facts-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Trusts Estates

### Asset Liability Inventory Builder

- **Skill file:** `skills/trusts-estates/asset-liability-inventory-builder/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a structured, source-cited inventory of estate or trust assets and liabilities for attorney review, without valuing assets.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/asset-liability-inventory-builder/SKILL.md
- Current skill name: Asset Liability Inventory Builder
- Practice area: Trusts Estates
- Current description: Use when building a structured, source-cited inventory of estate or trust assets and liabilities for attorney review, without valuing assets.
- Core purpose (must not change): Build a structured, source-cited inventory of estate or trust assets and liabilities — with owner/title, source, and any user-supplied value for each item — so a qualified attorney can review the estate's composition.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/asset-liability-inventory-builder/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Beneficiary Designation Review

- **Skill file:** `skills/trusts-estates/beneficiary-designation-review/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when reviewing beneficiary designations and account titling into a source-cited table and inconsistency list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/beneficiary-designation-review/SKILL.md
- Current skill name: Beneficiary Designation Review
- Practice area: Trusts Estates
- Current description: Use when reviewing beneficiary designations and account titling into a source-cited table and inconsistency list for attorney review.
- Core purpose (must not change): Review beneficiary designations, account titling, and transfer-on-death or payable-on-death forms into a source-cited designation table and an inconsistency list, so a qualified attorney can evaluate them against estate intent.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/beneficiary-designation-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Capacity Undue Influence Facts Organizer

- **Skill file:** `skills/trusts-estates/capacity-undue-influence-facts-organizer/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when organizing the facts relevant to capacity and undue-influence questions into a source-cited chronology for attorney review, without determining capacity or undue influence.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/capacity-undue-influence-facts-organizer/SKILL.md
- Current skill name: Capacity Undue Influence Facts Organizer
- Practice area: Trusts Estates
- Current description: Use when organizing the facts relevant to capacity and undue-influence questions into a source-cited chronology for attorney review, without determining capacity or undue influence.
- Core purpose (must not change): Organize the facts relevant to capacity and undue-influence questions into a source-cited chronology, a source table, and red-flag themes, so a qualified attorney can evaluate them.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/capacity-undue-influence-facts-organizer/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Estate Document Summary

- **Skill file:** `skills/trusts-estates/estate-document-summary/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when producing a source-cited summary of a will, trust, codicil, amendment, power of attorney, or advance directive for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/estate-document-summary/SKILL.md
- Current skill name: Estate Document Summary
- Practice area: Trusts Estates
- Current description: Use when producing a source-cited summary of a will, trust, codicil, amendment, power of attorney, or advance directive for attorney review.
- Core purpose (must not change): Produce a source-cited summary of a will, trust, codicil, trust amendment, power of attorney, advance directive, or related estate document — a key terms table, an ambiguity list, and verification items — so a qualified attorney can review the instrument.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/estate-document-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Estate Litigation Facts Chronology

- **Skill file:** `skills/trusts-estates/estate-litigation-facts-chronology/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a source-cited factual chronology for a will contest, trust contest, or fiduciary dispute for attorney review, without assessing the merits.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/estate-litigation-facts-chronology/SKILL.md
- Current skill name: Estate Litigation Facts Chronology
- Practice area: Trusts Estates
- Current description: Use when building a source-cited factual chronology for a will contest, trust contest, or fiduciary dispute for attorney review, without assessing the merits.
- Core purpose (must not change): Build a source-cited factual chronology for a will contest, trust contest, fiduciary dispute, accounting dispute, beneficiary dispute, or capacity / undue-influence matter, so a qualified attorney can work from an organized timeline.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/estate-litigation-facts-chronology/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Estate Planning Intake

- **Skill file:** `skills/trusts-estates/estate-planning-intake/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when capturing the facts of an estate-planning matter into a structured, source-cited working paper and planning issue map for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/estate-planning-intake/SKILL.md
- Current skill name: Estate Planning Intake
- Practice area: Trusts Estates
- Current description: Use when capturing the facts of an estate-planning matter into a structured, source-cited working paper and planning issue map for attorney review.
- Core purpose (must not change): Capture the facts of an estate-planning matter into a structured, source-cited working paper — an intake summary, a planning issue map, missing facts, a document request list, and verification questions — so a qualified, licensed attorney can plan the engagement.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/estate-planning-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Estate Tax Issue Intake

- **Skill file:** `skills/trusts-estates/estate-tax-issue-intake/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when capturing the facts of an estate, gift, GST, or inheritance tax matter into a source-cited issue map for tax professional review, without calculating taxes.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/estate-tax-issue-intake/SKILL.md
- Current skill name: Estate Tax Issue Intake
- Practice area: Trusts Estates
- Current description: Use when capturing the facts of an estate, gift, GST, or inheritance tax matter into a source-cited issue map for tax professional review, without calculating taxes.
- Core purpose (must not change): Capture the facts of an estate, gift, generation-skipping transfer (GST), or inheritance tax matter into a source-cited issue map, with missing facts, a document request list, and verification questions, so a qualified tax professional or attorney can evaluate the issues.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/estate-tax-issue-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Fiduciary Duty Issue Spotter

- **Skill file:** `skills/trusts-estates/fiduciary-duty-issue-spotter/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when issue-spotting potential fiduciary-duty questions from provided facts and documents into a source-cited issue matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/fiduciary-duty-issue-spotter/SKILL.md
- Current skill name: Fiduciary Duty Issue Spotter
- Practice area: Trusts Estates
- Current description: Use when issue-spotting potential fiduciary-duty questions from provided facts and documents into a source-cited issue matrix for attorney review.
- Core purpose (must not change): Issue-spot potential fiduciary-duty questions from provided facts and documents into a source-cited issue matrix, with missing facts, escalation items, and verification questions, so a qualified attorney can evaluate fiduciary conduct.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/fiduciary-duty-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Post Death Administration Task Tracker

- **Skill file:** `skills/trusts-estates/post-death-administration-task-tracker/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a source-cited post-death administration task tracker for attorney review, without calculating deadlines or approving distributions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/post-death-administration-task-tracker/SKILL.md
- Current skill name: Post Death Administration Task Tracker
- Practice area: Trusts Estates
- Current description: Use when building a source-cited post-death administration task tracker for attorney review, without calculating deadlines or approving distributions.
- Core purpose (must not change): Build a source-cited post-death administration task tracker — with a source, owner, status, dependency, and uncertainty flag for each task — so a qualified attorney can supervise estate or trust administration after a death.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/post-death-administration-task-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Probate Document Checklist

- **Skill file:** `skills/trusts-estates/probate-document-checklist/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a probate document checklist and missing-document list for attorney review, without preparing filing-ready probate forms.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/probate-document-checklist/SKILL.md
- Current skill name: Probate Document Checklist
- Practice area: Trusts Estates
- Current description: Use when building a probate document checklist and missing-document list for attorney review, without preparing filing-ready probate forms.
- Core purpose (must not change): Build a probate document checklist and missing-document list — with a status, source, and responsible party for each item — so a qualified attorney can assemble and review the probate record.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/probate-document-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Trust Administration Tracker

- **Skill file:** `skills/trusts-estates/trust-administration-tracker/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a source-cited trust administration task tracker for attorney review, without approving distributions or determining entitlement.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/trust-administration-tracker/SKILL.md
- Current skill name: Trust Administration Tracker
- Practice area: Trusts Estates
- Current description: Use when building a source-cited trust administration task tracker for attorney review, without approving distributions or determining entitlement.
- Core purpose (must not change): Build a source-cited trust administration task tracker — with a source, responsible party, status, dependency, and missing facts for each task — so a qualified attorney can supervise the administration.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/trust-administration-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Trust Funding Checklist

- **Skill file:** `skills/trusts-estates/trust-funding-checklist/SKILL.md`
- **Practice area:** Trusts Estates
- **Current description:** Use when building a checklist for funding or reviewing the funding of a trust into a source-cited tracker for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/trusts-estates/trust-funding-checklist/SKILL.md
- Current skill name: Trust Funding Checklist
- Practice area: Trusts Estates
- Current description: Use when building a checklist for funding or reviewing the funding of a trust into a source-cited tracker for attorney review.
- Core purpose (must not change): Build a checklist for funding, or reviewing the funding of, a trust — a source-cited tracker of which assets have been transferred, with the funding evidence and missing items — so a qualified attorney can review trust funding.

CONTEXT TO READ FIRST
- Read skills/trusts-estates/trust-funding-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Bankruptcy Restructuring

### Automatic Stay Issue Spotter

- **Skill file:** `skills/bankruptcy-restructuring/automatic-stay-issue-spotter/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when issue-spotting automatic stay concerns from user-provided facts into a source-cited stay-risk fact map for attorney review, without concluding whether the stay applies.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/automatic-stay-issue-spotter/SKILL.md
- Current skill name: Automatic Stay Issue Spotter
- Practice area: Bankruptcy Restructuring
- Current description: Use when issue-spotting automatic stay concerns from user-provided facts into a source-cited stay-risk fact map for attorney review, without concluding whether the stay applies.
- Core purpose (must not change): Issue-spot automatic stay concerns from user-provided facts into a source-cited stay-risk fact map and action inventory, with escalation flags and verification questions, so a qualified attorney can evaluate the automatic stay.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/automatic-stay-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Bankruptcy Deadline Tracker Intake

- **Skill file:** `skills/bankruptcy-restructuring/bankruptcy-deadline-tracker-intake/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when intaking user-provided bankruptcy dates and notices into a draft deadline tracker for attorney verification, without calculating any deadline.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/bankruptcy-deadline-tracker-intake/SKILL.md
- Current skill name: Bankruptcy Deadline Tracker Intake
- Practice area: Bankruptcy Restructuring
- Current description: Use when intaking user-provided bankruptcy dates and notices into a draft deadline tracker for attorney verification, without calculating any deadline.
- Core purpose (must not change): Intake the bankruptcy dates and notices a user provides into a draft deadline tracker — with the source, responsible party, status, and an uncertainty flag for each entry — so a qualified attorney can verify every date.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/bankruptcy-deadline-tracker-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Bankruptcy Diligence Request List

- **Skill file:** `skills/bankruptcy-restructuring/bankruptcy-diligence-request-list/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when generating a bankruptcy or distressed-transaction diligence request list organized by workstream for attorney-supervised diligence.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/bankruptcy-diligence-request-list/SKILL.md
- Current skill name: Bankruptcy Diligence Request List
- Practice area: Bankruptcy Restructuring
- Current description: Use when generating a bankruptcy or distressed-transaction diligence request list organized by workstream for attorney-supervised diligence.
- Core purpose (must not change): Generate a bankruptcy or distressed-transaction diligence request list, organized by workstream with priority, rationale, owner, and follow-up, so attorney-supervised diligence can request, track, and escalate the right documents.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/bankruptcy-diligence-request-list/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Bankruptcy Matter Intake

- **Skill file:** `skills/bankruptcy-restructuring/bankruptcy-matter-intake/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when capturing the facts of a bankruptcy or restructuring matter into a structured, source-cited working paper for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/bankruptcy-matter-intake/SKILL.md
- Current skill name: Bankruptcy Matter Intake
- Practice area: Bankruptcy Restructuring
- Current description: Use when capturing the facts of a bankruptcy or restructuring matter into a structured, source-cited working paper for attorney review.
- Core purpose (must not change): Capture the facts of a bankruptcy or restructuring matter into a structured, source-cited working paper — a matter summary, a fact register, risk themes, missing facts, a document request list, and verification questions — so a qualified, licensed attorney can evaluate the matter.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/bankruptcy-matter-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Cash Collateral DIP Financing Issue Spotter

- **Skill file:** `skills/bankruptcy-restructuring/cash-collateral-dip-financing-issue-spotter/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when issue-spotting a cash collateral or DIP financing document into a source-cited key terms table and issue list for attorney review, without approving terms or determining lien priority.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/cash-collateral-dip-financing-issue-spotter/SKILL.md
- Current skill name: Cash Collateral DIP Financing Issue Spotter
- Practice area: Bankruptcy Restructuring
- Current description: Use when issue-spotting a cash collateral or DIP financing document into a source-cited key terms table and issue list for attorney review, without approving terms or determining lien priority.
- Core purpose (must not change): Issue-spot a cash collateral or debtor-in-possession (DIP) financing document into a source-cited key terms table and issue list, with missing facts, business and legal questions, and a verification checklist, so a qualified attorney can evaluate the document.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/cash-collateral-dip-financing-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Creditor Claim Intake

- **Skill file:** `skills/bankruptcy-restructuring/creditor-claim-intake/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when organizing a creditor's facts and documents for a potential bankruptcy claim into a source-cited claim facts table for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/creditor-claim-intake/SKILL.md
- Current skill name: Creditor Claim Intake
- Practice area: Bankruptcy Restructuring
- Current description: Use when organizing a creditor's facts and documents for a potential bankruptcy claim into a source-cited claim facts table for attorney review.
- Core purpose (must not change): Organize a creditor's facts and documents for a potential bankruptcy claim into a structured, source-cited claim facts table — with a document request list, missing facts, dispute flags, and verification questions — so a qualified attorney can evaluate the claim.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/creditor-claim-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Distressed Asset Sale Checklist

- **Skill file:** `skills/bankruptcy-restructuring/distressed-asset-sale-checklist/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when building a bankruptcy or distressed asset-sale checklist, contract/cure tracker, and closing-deliverables tracker for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/distressed-asset-sale-checklist/SKILL.md
- Current skill name: Distressed Asset Sale Checklist
- Practice area: Bankruptcy Restructuring
- Current description: Use when building a bankruptcy or distressed asset-sale checklist, contract/cure tracker, and closing-deliverables tracker for attorney review.
- Core purpose (must not change): Build a checklist for a bankruptcy or distressed asset sale — a sale checklist, a diligence request list, a contract/cure tracker, and a closing-deliverables tracker — so a qualified attorney can run and review the sale process.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/distressed-asset-sale-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Executory Contract Assumption Rejection Checklist

- **Skill file:** `skills/bankruptcy-restructuring/executory-contract-assumption-rejection-checklist/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when organizing executory contract and unexpired lease facts into a source-cited contract status table and assumption/rejection issue list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/executory-contract-assumption-rejection-checklist/SKILL.md
- Current skill name: Executory Contract Assumption Rejection Checklist
- Practice area: Bankruptcy Restructuring
- Current description: Use when organizing executory contract and unexpired lease facts into a source-cited contract status table and assumption/rejection issue list for attorney review.
- Core purpose (must not change): Organize executory contract and unexpired lease facts into a source-cited contract status table, a cure/default tracker, and an assumption/rejection issue list, so a qualified attorney can evaluate assumption, rejection, and assignment.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/executory-contract-assumption-rejection-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Plan Disclosure Statement Issue Spotter

- **Skill file:** `skills/bankruptcy-restructuring/plan-disclosure-statement-issue-spotter/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when issue-spotting a Chapter 11 plan and disclosure statement into a source-cited treatment table and issue list for attorney review, without concluding confirmability.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/plan-disclosure-statement-issue-spotter/SKILL.md
- Current skill name: Plan Disclosure Statement Issue Spotter
- Practice area: Bankruptcy Restructuring
- Current description: Use when issue-spotting a Chapter 11 plan and disclosure statement into a source-cited treatment table and issue list for attorney review, without concluding confirmability.
- Core purpose (must not change): Issue-spot a Chapter 11 plan and disclosure statement into a source-cited treatment table, an issue list, and a consistency-issues list, so a qualified attorney can evaluate the documents.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/plan-disclosure-statement-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Preference Demand Response Triage

- **Skill file:** `skills/bankruptcy-restructuring/preference-demand-response-triage/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when organizing the facts for responding to a preference demand into a source-cited transfer timeline and defense-facts checklist for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/preference-demand-response-triage/SKILL.md
- Current skill name: Preference Demand Response Triage
- Practice area: Bankruptcy Restructuring
- Current description: Use when organizing the facts for responding to a preference demand into a source-cited transfer timeline and defense-facts checklist for attorney review.
- Core purpose (must not change): Organize the facts for responding to a preference demand into a source-cited transfer timeline and defense-facts checklist, with missing documents, response-planning issues, and verification questions, so a qualified attorney can evaluate the demand and a response.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/preference-demand-response-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Proof of Claim Checklist

- **Skill file:** `skills/bankruptcy-restructuring/proof-of-claim-checklist/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when building a proof-of-claim preparation checklist and supporting-document tracker for attorney review, without preparing a filing-ready claim.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/proof-of-claim-checklist/SKILL.md
- Current skill name: Proof of Claim Checklist
- Practice area: Bankruptcy Restructuring
- Current description: Use when building a proof-of-claim preparation checklist and supporting-document tracker for attorney review, without preparing a filing-ready claim.
- Core purpose (must not change): Build a proof-of-claim preparation checklist and supporting-document tracker so a qualified attorney can prepare and review a proof of claim.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/proof-of-claim-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Restructuring Term Sheet Review

- **Skill file:** `skills/bankruptcy-restructuring/restructuring-term-sheet-review/SKILL.md`
- **Practice area:** Bankruptcy Restructuring
- **Current description:** Use when reviewing a restructuring support agreement, forbearance agreement, workout term sheet, or related restructuring document into a source-cited key terms table and issue list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/bankruptcy-restructuring/restructuring-term-sheet-review/SKILL.md
- Current skill name: Restructuring Term Sheet Review
- Practice area: Bankruptcy Restructuring
- Current description: Use when reviewing a restructuring support agreement, forbearance agreement, workout term sheet, or related restructuring document into a source-cited key terms table and issue list for attorney review.
- Core purpose (must not change): Review a restructuring support agreement, forbearance agreement, exchange offer, workout term sheet, lender proposal, plan support term sheet, or restructuring transaction summary into a source-cited key terms table, an issue list, a risk matrix, and negotiation points, so a qualified attorney can evaluate the document from the user's perspective.

CONTEXT TO READ FIRST
- Read skills/bankruptcy-restructuring/restructuring-term-sheet-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Insurance

### Bad Faith Risk Triage

- **Skill file:** `skills/insurance/bad-faith-risk-triage/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when issue-spotting potential claim-handling and bad-faith risk themes from claim file materials into a source-cited risk-theme list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/bad-faith-risk-triage/SKILL.md
- Current skill name: Bad Faith Risk Triage
- Practice area: Insurance
- Current description: Use when issue-spotting potential claim-handling and bad-faith risk themes from claim file materials into a source-cited risk-theme list for attorney review.
- Core purpose (must not change): Issue-spot potential claim-handling and bad-faith risk **themes** from claim file materials — investigation timeline, communications, delays, coverage explanations, information requests, settlement demands, defense handling, conflicts, documentation, and escalation — into a source-cited risk-theme list for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/bad-faith-risk-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Certificate of Insurance Review

- **Skill file:** `skills/insurance/certificate-of-insurance-review/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when reviewing certificates of insurance and related endorsements against contract insurance requirements into a source-cited comparison table for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/certificate-of-insurance-review/SKILL.md
- Current skill name: Certificate of Insurance Review
- Practice area: Insurance
- Current description: Use when reviewing certificates of insurance and related endorsements against contract insurance requirements into a source-cited comparison table for attorney review.
- Core purpose (must not change): Review one or more certificates of insurance (COIs) and related endorsements — additional insured schedules, waiver of subrogation endorsements, primary and noncontributory endorsements — against contract insurance requirements where provided, into a source-cited comparison table, a missing-endorsement list, and a mismatch list for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/certificate-of-insurance-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Claims Chronology Builder

- **Skill file:** `skills/insurance/claims-chronology-builder/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when building a source-cited claim chronology from notices, correspondence, adjuster notes, pleadings, demands, and payment history for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/claims-chronology-builder/SKILL.md
- Current skill name: Claims Chronology Builder
- Practice area: Insurance
- Current description: Use when building a source-cited claim chronology from notices, correspondence, adjuster notes, pleadings, demands, and payment history for attorney review.
- Core purpose (must not change): Build a source-cited chronology of an insurance claim — from notices, correspondence, adjuster notes, pleadings, demands, policy documents, medical or property records if provided, and payment history — so a qualified attorney has an organized factual timeline for coverage, claim-handling, or litigation review.

CONTEXT TO READ FIRST
- Read skills/insurance/claims-chronology-builder/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Coverage Issue Spotter

- **Skill file:** `skills/insurance/coverage-issue-spotter/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when issue-spotting insurance coverage questions from a policy, claim facts, tender, pleadings, and correspondence into a source-cited coverage issue matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/coverage-issue-spotter/SKILL.md
- Current skill name: Coverage Issue Spotter
- Practice area: Insurance
- Current description: Use when issue-spotting insurance coverage questions from a policy, claim facts, tender, pleadings, and correspondence into a source-cited coverage issue matrix for attorney review.
- Core purpose (must not change): Issue-spot the insurance coverage questions raised by a policy and a claim — from the policy, claim facts, tender, pleadings, demand letters, denial letters, and correspondence — into a source-cited coverage issue matrix for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/coverage-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Coverage Position Outline

- **Skill file:** `skills/insurance/coverage-position-outline/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when assembling a draft coverage-position outline from supplied policy and claim materials for a coverage attorney to develop and decide.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/coverage-position-outline/SKILL.md
- Current skill name: Coverage Position Outline
- Practice area: Insurance
- Current description: Use when assembling a draft coverage-position outline from supplied policy and claim materials for a coverage attorney to develop and decide.
- Core purpose (must not change): Assemble a structured, source-cited **coverage-position outline** from supplied policy and claim materials — facts, policy provisions, potential coverage grants, exclusions, endorsements, conditions, notice and cooperation, posture, and open issues — so a coverage attorney can develop and decide the position.

CONTEXT TO READ FIRST
- Read skills/insurance/coverage-position-outline/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Insurance Policy Summary

- **Skill file:** `skills/insurance/insurance-policy-summary/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when summarizing an insurance policy into a source-cited overview of declarations, coverage parts, limits, exclusions, endorsements, and conditions for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/insurance-policy-summary/SKILL.md
- Current skill name: Insurance Policy Summary
- Practice area: Insurance
- Current description: Use when summarizing an insurance policy into a source-cited overview of declarations, coverage parts, limits, exclusions, endorsements, and conditions for attorney review.
- Core purpose (must not change): Summarize an insurance policy into a structured, source-cited overview — declarations, named and additional insureds, policy period, coverage parts, limits, deductibles and self-insured retentions (SIRs), insuring agreements, definitions, exclusions, endorsements, conditions, and the forms schedule — so a qualified attorney can review the policy efficiently.

CONTEXT TO READ FIRST
- Read skills/insurance/insurance-policy-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Insurance Requirements Contract Review

- **Skill file:** `skills/insurance/insurance-requirements-contract-review/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when reviewing the insurance and indemnity clauses of a contract, lease, or MSA into a source-cited requirements table and risk matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/insurance-requirements-contract-review/SKILL.md
- Current skill name: Insurance Requirements Contract Review
- Practice area: Insurance
- Current description: Use when reviewing the insurance and indemnity clauses of a contract, lease, or MSA into a source-cited requirements table and risk matrix for attorney review.
- Core purpose (must not change): Review the insurance and indemnity provisions of a contract — lease, master services agreement, vendor agreement, construction agreement, service agreement, or purchase agreement — into a source-cited requirements table, a risk matrix, a missing-provisions list, and negotiation points for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/insurance-requirements-contract-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Insurer Insured Communications Review

- **Skill file:** `skills/insurance/insurer-insured-communications-review/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when reviewing insurer, insured, claimant, or broker communications for clarity, consistency, privilege concerns, and claim-handling risk for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/insurer-insured-communications-review/SKILL.md
- Current skill name: Insurer Insured Communications Review
- Practice area: Insurance
- Current description: Use when reviewing insurer, insured, claimant, or broker communications for clarity, consistency, privilege concerns, and claim-handling risk for attorney review.
- Core purpose (must not change): Review insurer, insured, claimant, or broker communications — letters, emails, and claim notes — for clarity, consistency, privilege and confidentiality concerns, claim-handling risk, reservation/denial posture, information requests, and escalation needs, into a source-cited issue list and suggested attorney-review edits.

CONTEXT TO READ FIRST
- Read skills/insurance/insurer-insured-communications-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Policy Renewal Placement Diligence Checklist

- **Skill file:** `skills/insurance/policy-renewal-placement-diligence-checklist/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when generating a legal and compliance diligence checklist for an insurance policy renewal or placement for attorney and broker review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/policy-renewal-placement-diligence-checklist/SKILL.md
- Current skill name: Policy Renewal Placement Diligence Checklist
- Practice area: Insurance
- Current description: Use when generating a legal and compliance diligence checklist for an insurance policy renewal or placement for attorney and broker review.
- Core purpose (must not change): Generate a legal and compliance-oriented diligence checklist for an insurance policy renewal or new placement — covering expiring policies, claims history, material changes, contracts requiring coverage, new operations and jurisdictions, regulatory issues if supplied, additional insured obligations, coverage gaps, exclusions, endorsements, limits, deductibles and SIRs, and open-claim implicatio...

CONTEXT TO READ FIRST
- Read skills/insurance/policy-renewal-placement-diligence-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Reservation of Rights Review

- **Skill file:** `skills/insurance/reservation-of-rights-review/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when reviewing a reservation of rights letter or coverage correspondence into a source-cited issue list and provision-reference table for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/reservation-of-rights-review/SKILL.md
- Current skill name: Reservation of Rights Review
- Practice area: Insurance
- Current description: Use when reviewing a reservation of rights letter or coverage correspondence into a source-cited issue list and provision-reference table for attorney review.
- Core purpose (must not change): Review a reservation of rights (ROR) letter or related coverage correspondence into a source-cited issue list and a provision-reference table, so a qualified attorney can evaluate what the letter says, what it relies on, and what is unclear.

CONTEXT TO READ FIRST
- Read skills/insurance/reservation-of-rights-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Subrogation Recovery Tracker

- **Skill file:** `skills/insurance/subrogation-recovery-tracker/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when organizing potential subrogation, reimbursement, salvage, contribution, and recovery facts from a loss into a source-cited recovery fact map for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/subrogation-recovery-tracker/SKILL.md
- Current skill name: Subrogation Recovery Tracker
- Practice area: Insurance
- Current description: Use when organizing potential subrogation, reimbursement, salvage, contribution, and recovery facts from a loss into a source-cited recovery fact map for attorney review.
- Core purpose (must not change): Organize the potential subrogation, reimbursement, salvage, contribution, indemnity, and recovery facts arising from a loss — loss facts, responsible parties, contracts, indemnity provisions, policy subrogation provisions, payments made, evidence preservation, notices, and litigation status — into a source-cited recovery fact map for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/subrogation-recovery-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Tender Letter Review

- **Skill file:** `skills/insurance/tender-letter-review/SKILL.md`
- **Practice area:** Insurance
- **Current description:** Use when reviewing a tender letter, notice of claim, or additional insured or contractual indemnity tender into a completeness checklist and risk flags for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/insurance/tender-letter-review/SKILL.md
- Current skill name: Tender Letter Review
- Practice area: Insurance
- Current description: Use when reviewing a tender letter, notice of claim, or additional insured or contractual indemnity tender into a completeness checklist and risk flags for attorney review.
- Core purpose (must not change): Review a tender letter, notice of claim, additional insured tender, or contractual indemnity tender into a completeness checklist, a missing-documents list, risk flags, and proposed revisions for attorney review.

CONTEXT TO READ FIRST
- Read skills/insurance/tender-letter-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Securities Capital Markets

### Capital Markets Closing Checklist

- **Skill file:** `skills/securities-capital-markets/capital-markets-closing-checklist/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing capital markets closing checklist as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/capital-markets-closing-checklist/SKILL.md
- Current skill name: Capital Markets Closing Checklist
- Practice area: Securities Capital Markets
- Current description: Use when performing capital markets closing checklist as draft work product for attorney review.
- Core purpose (must not change): Build a capital-markets closing deliverables tracker with ownership, dependencies, and verification gates.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/capital-markets-closing-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Comfort and Backup Request Tracker

- **Skill file:** `skills/securities-capital-markets/comfort-backup-request-tracker/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing comfort and backup tracker as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/comfort-backup-request-tracker/SKILL.md
- Current skill name: Comfort and Backup Request Tracker
- Practice area: Securities Capital Markets
- Current description: Use when performing comfort and backup tracker as draft work product for attorney review.
- Core purpose (must not change): Create a numbered tracker of factual statements potentially requiring comfort or backup support.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/comfort-backup-request-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Form D and Blue Sky Tracker

- **Skill file:** `skills/securities-capital-markets/form-d-blue-sky-tracker/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing Form D and blue sky tracking as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/form-d-blue-sky-tracker/SKILL.md
- Current skill name: Form D and Blue Sky Tracker
- Practice area: Securities Capital Markets
- Current description: Use when performing Form D and blue sky tracking as draft work product for attorney review.
- Core purpose (must not change): Build a source-grounded tracker for Form D and state notice workflow facts without determining obligations or deadlines.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/form-d-blue-sky-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Insider Trading Policy Review

- **Skill file:** `skills/securities-capital-markets/insider-trading-policy-review/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing insider trading policy review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/insider-trading-policy-review/SKILL.md
- Current skill name: Insider Trading Policy Review
- Practice area: Securities Capital Markets
- Current description: Use when performing insider trading policy review as draft work product for attorney review.
- Core purpose (must not change): Review insider-trading policy documents for covered-person scope, procedure gaps, and ambiguities without approving trades.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/insider-trading-policy-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Investor Rights Agreement Review

- **Skill file:** `skills/securities-capital-markets/investor-rights-agreement-review/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing investor rights agreement review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/investor-rights-agreement-review/SKILL.md
- Current skill name: Investor Rights Agreement Review
- Practice area: Securities Capital Markets
- Current description: Use when performing investor rights agreement review as draft work product for attorney review.
- Core purpose (must not change): Review investor-side financing governance and rights documents for key terms, gaps, conflicts, and negotiation points.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/investor-rights-agreement-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Offering Document Disclosure Review

- **Skill file:** `skills/securities-capital-markets/offering-document-disclosure-review/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing offering disclosure review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/offering-document-disclosure-review/SKILL.md
- Current skill name: Offering Document Disclosure Review
- Practice area: Securities Capital Markets
- Current description: Use when performing offering disclosure review as draft work product for attorney review.
- Core purpose (must not change): Review offering disclosure documents for completeness, consistency, and source-cited issue spotting without certifying compliance.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/offering-document-disclosure-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Private Placement Checklist

- **Skill file:** `skills/securities-capital-markets/private-placement-checklist/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing private placement checklist as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/private-placement-checklist/SKILL.md
- Current skill name: Private Placement Checklist
- Practice area: Securities Capital Markets
- Current description: Use when performing private placement checklist as draft work product for attorney review.
- Core purpose (must not change): Build a private-placement process checklist by workstream, ownership, priority, source, gaps, and attorney-verification items.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/private-placement-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Public Company Reporting Calendar Intake

- **Skill file:** `skills/securities-capital-markets/public-company-reporting-calendar-intake/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing public company reporting calendar intake as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/public-company-reporting-calendar-intake/SKILL.md
- Current skill name: Public Company Reporting Calendar Intake
- Practice area: Securities Capital Markets
- Current description: Use when performing public company reporting calendar intake as draft work product for attorney review.
- Core purpose (must not change): Intake facts needed to draft or review a public-company reporting workflow calendar without computing deadlines.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/public-company-reporting-calendar-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Risk Factor Review

- **Skill file:** `skills/securities-capital-markets/risk-factor-review/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing risk factor review as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/risk-factor-review/SKILL.md
- Current skill name: Risk Factor Review
- Practice area: Securities Capital Markets
- Current description: Use when performing risk factor review as draft work product for attorney review.
- Core purpose (must not change): Review risk-factor sections for specificity, issuer fit, duplication, and cross-document consistency without concluding sufficiency.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/risk-factor-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### SEC Filing Consistency Check

- **Skill file:** `skills/securities-capital-markets/sec-filing-consistency-check/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing SEC filing consistency as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/sec-filing-consistency-check/SKILL.md
- Current skill name: SEC Filing Consistency Check
- Practice area: Securities Capital Markets
- Current description: Use when performing SEC filing consistency as draft work product for attorney review.
- Core purpose (must not change): Compare user-provided SEC filing drafts for internal and cross-document consistency with source-cited discrepancies.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/sec-filing-consistency-check/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Section 16 and Beneficial Ownership Triage

- **Skill file:** `skills/securities-capital-markets/section-16-beneficial-ownership-triage/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing section 16 and beneficial ownership triage as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/section-16-beneficial-ownership-triage/SKILL.md
- Current skill name: Section 16 and Beneficial Ownership Triage
- Practice area: Securities Capital Markets
- Current description: Use when performing section 16 and beneficial ownership triage as draft work product for attorney review.
- Core purpose (must not change): Organize facts for Section 16 and beneficial-ownership filing analysis without final status conclusions.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/section-16-beneficial-ownership-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Securities Exemption Issue Spotter

- **Skill file:** `skills/securities-capital-markets/securities-exemption-issue-spotter/SKILL.md`
- **Practice area:** Securities Capital Markets
- **Current description:** Use when performing securities exemption issue spotting as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/securities-capital-markets/securities-exemption-issue-spotter/SKILL.md
- Current skill name: Securities Exemption Issue Spotter
- Practice area: Securities Capital Markets
- Current description: Use when performing securities exemption issue spotting as draft work product for attorney review.
- Core purpose (must not change): Issue-spot potential exemption pathways and missing facts for private offerings without concluding exemption availability.

CONTEXT TO READ FIRST
- Read skills/securities-capital-markets/securities-exemption-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Real Estate

### Closing Deliverables Tracker

- **Skill file:** `skills/real-estate/closing-deliverables-tracker/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when building a closing checklist of deliverables for a real estate transaction, tracking responsible party, status, and dependencies.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/closing-deliverables-tracker/SKILL.md
- Current skill name: Closing Deliverables Tracker
- Practice area: Real Estate
- Current description: Use when building a closing checklist of deliverables for a real estate transaction, tracking responsible party, status, and dependencies.
- Core purpose (must not change): Build a structured closing-deliverables tracker for a real estate transaction: a working checklist that lists each document or item required to close, identifies the party responsible for it, records its status, and surfaces the dependencies and open issues that must be resolved before closing.

CONTEXT TO READ FIRST
- Read skills/real-estate/closing-deliverables-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Commercial Lease Review

- **Skill file:** `skills/real-estate/commercial-lease-review/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when reviewing a commercial lease from a specified party's perspective to spot business and legal issues and produce a risk matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/commercial-lease-review/SKILL.md
- Current skill name: Commercial Lease Review
- Practice area: Real Estate
- Current description: Use when reviewing a commercial lease from a specified party's perspective to spot business and legal issues and produce a risk matrix for attorney review.
- Core purpose (must not change): Review a commercial lease from a single, specified party's perspective, spot the business and legal issues that perspective should care about, and organize them into a risk matrix and a prioritized issue list that an attorney can work from.

CONTEXT TO READ FIRST
- Read skills/real-estate/commercial-lease-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Estoppel and SNDA Review

- **Skill file:** `skills/real-estate/estoppel-snda-review/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when reviewing tenant or landlord estoppel certificates, SNDAs, and recognition agreements, and comparing them against the lease where provided.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/estoppel-snda-review/SKILL.md
- Current skill name: Estoppel and SNDA Review
- Practice area: Real Estate
- Current description: Use when reviewing tenant or landlord estoppel certificates, SNDAs, and recognition agreements, and comparing them against the lease where provided.
- Core purpose (must not change): Produce a structured, attorney-ready review of an estoppel certificate, a subordination, non-disturbance and attornment agreement (SNDA), or a recognition agreement, from the perspective of the lender, tenant, or landlord.

CONTEXT TO READ FIRST
- Read skills/real-estate/estoppel-snda-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Lease Abstract

- **Skill file:** `skills/real-estate/lease-abstract/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when extracting the key business and legal terms of a commercial lease into a structured, source-cited abstract for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/lease-abstract/SKILL.md
- Current skill name: Lease Abstract
- Practice area: Real Estate
- Current description: Use when extracting the key business and legal terms of a commercial lease into a structured, source-cited abstract for attorney review.
- Core purpose (must not change): Extract the key business and legal terms of a commercial lease into a structured, consistently formatted abstract that an attorney, asset manager, or transaction team can use as a working reference.

CONTEXT TO READ FIRST
- Read skills/real-estate/lease-abstract/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Lease Amendment Reconciliation

- **Skill file:** `skills/real-estate/lease-amendment-reconciliation/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when reconciling a base lease against its amendments, side letters, assignments, and related documents to determine the current controlling terms.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/lease-amendment-reconciliation/SKILL.md
- Current skill name: Lease Amendment Reconciliation
- Practice area: Real Estate
- Current description: Use when reconciling a base lease against its amendments, side letters, assignments, and related documents to determine the current controlling terms.
- Core purpose (must not change): Reconcile a base commercial lease against its amendments, side letters, assignments, guaranties, estoppels, and related documents to produce a single working view of what each material lease term currently states.

CONTEXT TO READ FIRST
- Read skills/real-estate/lease-amendment-reconciliation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Purchase and Sale Agreement Review

- **Skill file:** `skills/real-estate/psa-review/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when reviewing a real estate purchase and sale agreement from a specified party's perspective to spot issues and produce a risk matrix for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/psa-review/SKILL.md
- Current skill name: Purchase and Sale Agreement Review
- Practice area: Real Estate
- Current description: Use when reviewing a real estate purchase and sale agreement from a specified party's perspective to spot issues and produce a risk matrix for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready issue-spotting review of a real estate purchase and sale agreement (PSA) from a single party's perspective.

CONTEXT TO READ FIRST
- Read skills/real-estate/psa-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Real Estate Diligence Checklist

- **Skill file:** `skills/real-estate/real-estate-diligence-checklist/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when generating a tailored real estate due-diligence checklist based on the transaction type, property type, jurisdiction, party role, and document set.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/real-estate-diligence-checklist/SKILL.md
- Current skill name: Real Estate Diligence Checklist
- Practice area: Real Estate
- Current description: Use when generating a tailored real estate due-diligence checklist based on the transaction type, property type, jurisdiction, party role, and document set.
- Core purpose (must not change): Generate a structured, tailored due-diligence checklist for a real estate transaction so an attorney and a transaction team have an organized scaffold for the diligence period.

CONTEXT TO READ FIRST
- Read skills/real-estate/real-estate-diligence-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Title and Survey Objection Tracker

- **Skill file:** `skills/real-estate/title-survey-objection-tracker/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when organizing title commitment exceptions and survey matters into a tracked, source-cited objection list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/title-survey-objection-tracker/SKILL.md
- Current skill name: Title and Survey Objection Tracker
- Practice area: Real Estate
- Current description: Use when organizing title commitment exceptions and survey matters into a tracked, source-cited objection list for attorney review.
- Core purpose (must not change): Organize the exceptions raised in a title commitment or title report, together with the matters shown on a survey, into a single structured tracker that an attorney or transaction team can use as a working diligence reference.

CONTEXT TO READ FIRST
- Read skills/real-estate/title-survey-objection-tracker/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Zoning and Use Restriction Issue Spotter

- **Skill file:** `skills/real-estate/zoning-use-restriction-issue-spotter/SKILL.md`
- **Practice area:** Real Estate
- **Current description:** Use when issue-spotting zoning, permitted-use, recorded-restriction, and use-clause concerns from provided materials and producing questions for local counsel.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/real-estate/zoning-use-restriction-issue-spotter/SKILL.md
- Current skill name: Zoning and Use Restriction Issue Spotter
- Practice area: Real Estate
- Current description: Use when issue-spotting zoning, permitted-use, recorded-restriction, and use-clause concerns from provided materials and producing questions for local counsel.
- Core purpose (must not change): Read the materials a user provides about a property and its intended use, and surface the zoning, permitted-use, recorded-restriction, and use-clause concerns those materials raise.

CONTEXT TO READ FIRST
- Read skills/real-estate/zoning-use-restriction-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Tax

### Crypto Digital Asset Tax Intake

- **Skill file:** `skills/tax/crypto-digital-asset-tax-intake/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when intaking crypto and digital-asset activity and organizing wallet, exchange, and transaction records into a source-cited intake matrix for tax professional review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/crypto-digital-asset-tax-intake/SKILL.md
- Current skill name: Crypto Digital Asset Tax Intake
- Practice area: Tax
- Current description: Use when intaking crypto and digital-asset activity and organizing wallet, exchange, and transaction records into a source-cited intake matrix for tax professional review.
- Core purpose (must not change): Intake a taxpayer's crypto and digital-asset activity and organize the wallet, exchange, and transaction records into a source-cited intake matrix by activity category, with missing records, uncertainty flags, and verification questions, so a qualified tax professional can evaluate treatment.

CONTEXT TO READ FIRST
- Read skills/tax/crypto-digital-asset-tax-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Employment Tax Worker Classification Intake

- **Skill file:** `skills/tax/employment-tax-worker-classification-intake/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when intaking worker, engagement, and payroll facts into a source-cited facts-to-verify table for employment-tax and worker-classification review by qualified counsel.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/employment-tax-worker-classification-intake/SKILL.md
- Current skill name: Employment Tax Worker Classification Intake
- Practice area: Tax
- Current description: Use when intaking worker, engagement, and payroll facts into a source-cited facts-to-verify table for employment-tax and worker-classification review by qualified counsel.
- Core purpose (must not change): Intake a taxpayer's worker, engagement, and payroll facts into a disciplined, source-cited facts-to-verify table so qualified employment and tax counsel can evaluate worker classification and employment-tax treatment.

CONTEXT TO READ FIRST
- Read skills/tax/employment-tax-worker-classification-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Entity Tax Classification Checklist

- **Skill file:** `skills/tax/entity-tax-classification-checklist/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when organizing entity formation, ownership, and election facts into a source-cited facts table so qualified tax counsel can evaluate tax classification.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/entity-tax-classification-checklist/SKILL.md
- Current skill name: Entity Tax Classification Checklist
- Practice area: Tax
- Current description: Use when organizing entity formation, ownership, and election facts into a source-cited facts table so qualified tax counsel can evaluate tax classification.
- Core purpose (must not change): Organize an entity's formation, ownership, election, and governance facts into a disciplined, source-cited facts table so qualified tax counsel can evaluate its tax classification.

CONTEXT TO READ FIRST
- Read skills/tax/entity-tax-classification-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### International Tax Issue Spotter

- **Skill file:** `skills/tax/international-tax-issue-spotter/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when issue-spotting cross-border tax questions into a source-cited issue map and missing-facts list for tax counsel, without concluding treaty, withholding, PE, or CFC/PFIC treatment.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/international-tax-issue-spotter/SKILL.md
- Current skill name: International Tax Issue Spotter
- Practice area: Tax
- Current description: Use when issue-spotting cross-border tax questions into a source-cited issue map and missing-facts list for tax counsel, without concluding treaty, withholding, PE, or CFC/PFIC treatment.
- Core purpose (must not change): Issue-spot the cross-border tax questions raised by a structure or transaction into a source-cited issue map, with the jurisdictions involved, missing facts, and questions for tax counsel.

CONTEXT TO READ FIRST
- Read skills/tax/international-tax-issue-spotter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Sales Use Tax Nexus Triage

- **Skill file:** `skills/tax/sales-use-tax-nexus-triage/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when intake-mapping sales and use tax nexus facts by jurisdiction into a source-cited fact map and open-question tracker for tax professional review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/sales-use-tax-nexus-triage/SKILL.md
- Current skill name: Sales Use Tax Nexus Triage
- Practice area: Tax
- Current description: Use when intake-mapping sales and use tax nexus facts by jurisdiction into a source-cited fact map and open-question tracker for tax professional review.
- Core purpose (must not change): Intake-map a taxpayer's sales and use tax facts jurisdiction by jurisdiction into a source-cited fact map and open-question tracker, so a qualified tax professional can evaluate nexus, taxability, and registration.

CONTEXT TO READ FIRST
- Read skills/tax/sales-use-tax-nexus-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Tax Covenants Indemnities Review

- **Skill file:** `skills/tax/tax-covenants-indemnities-review/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when reviewing the tax covenants and indemnities of a transaction agreement and mapping their architecture and negotiation issues for tax counsel verification.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/tax-covenants-indemnities-review/SKILL.md
- Current skill name: Tax Covenants Indemnities Review
- Practice area: Tax
- Current description: Use when reviewing the tax covenants and indemnities of a transaction agreement and mapping their architecture and negotiation issues for tax counsel verification.
- Core purpose (must not change): Review the tax covenants and indemnities of a transaction agreement and map their architecture, issues, and negotiation points — all source-cited — so tax counsel can verify the tax-risk allocation from the user's perspective.

CONTEXT TO READ FIRST
- Read skills/tax/tax-covenants-indemnities-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Tax Document Organizer

- **Skill file:** `skills/tax/tax-document-organizer/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when organizing a tax-related document set into a source-cited inventory with masked identifiers, missing-document list, and reviewer notes for supervised tax review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/tax-document-organizer/SKILL.md
- Current skill name: Tax Document Organizer
- Practice area: Tax
- Current description: Use when organizing a tax-related document set into a source-cited inventory with masked identifiers, missing-document list, and reviewer notes for supervised tax review.
- Core purpose (must not change): Organize a tax-related document set into a source-cited inventory — with masked references to sensitive identifiers, a missing-document list, uncertainty flags, and reviewer notes — so a tax professional can review an ordered, auditable record.

CONTEXT TO READ FIRST
- Read skills/tax/tax-document-organizer/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Tax Issue Intake

- **Skill file:** `skills/tax/tax-issue-intake/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when capturing and structuring the facts of a tax-sensitive matter or transaction into a source-cited working paper and tax issue map for qualified tax professional review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/tax-issue-intake/SKILL.md
- Current skill name: Tax Issue Intake
- Practice area: Tax
- Current description: Use when capturing and structuring the facts of a tax-sensitive matter or transaction into a source-cited working paper and tax issue map for qualified tax professional review.
- Core purpose (must not change): Capture and structure the facts of a tax-sensitive matter or transaction into a disciplined, source-cited working paper — an intake summary, a tax issue map, missing facts, a document request list, and verification questions — so qualified tax counsel or a licensed tax professional can evaluate treatment.

CONTEXT TO READ FIRST
- Read skills/tax/tax-issue-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Tax Provision Review Checklist

- **Skill file:** `skills/tax/tax-provision-review-checklist/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when reviewing the tax provisions of a contract and producing a source-cited issue checklist and negotiation-point list for tax professional review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/tax-provision-review-checklist/SKILL.md
- Current skill name: Tax Provision Review Checklist
- Practice area: Tax
- Current description: Use when reviewing the tax provisions of a contract and producing a source-cited issue checklist and negotiation-point list for tax professional review.
- Core purpose (must not change): Review the tax provisions of a contract or agreement and produce a source-cited key-terms table, a provision risk matrix, a missing-provisions list, and negotiation points, so a qualified tax professional can evaluate the tax terms from the user's perspective.

CONTEXT TO READ FIRST
- Read skills/tax/tax-provision-review-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Transaction Tax Diligence Request List

- **Skill file:** `skills/tax/transaction-tax-diligence-request-list/SKILL.md`
- **Practice area:** Tax
- **Current description:** Use when building a transaction tax diligence request list and follow-up tracker organized by tax workstream for attorney-supervised diligence.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/tax/transaction-tax-diligence-request-list/SKILL.md
- Current skill name: Transaction Tax Diligence Request List
- Practice area: Tax
- Current description: Use when building a transaction tax diligence request list and follow-up tracker organized by tax workstream for attorney-supervised diligence.
- Core purpose (must not change): Build a transaction tax diligence request list and follow-up tracker, organized by tax workstream, so attorney-supervised diligence can request, track, and escalate the right documents.

CONTEXT TO READ FIRST
- Read skills/tax/transaction-tax-diligence-request-list/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Financial Crime

### KYC Onboarding Review

- **Skill file:** `skills/financial-crime/kyc-onboarding-review/SKILL.md`
- **Practice area:** Financial Crime
- **Current description:** Use when reviewing a client or investor onboarding packet to inventory documents, extract KYC fields, apply the firm's KYC/AML rules grid, propose a customer risk rating, and assemble an escalation packet for compliance and attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/financial-crime/kyc-onboarding-review/SKILL.md
- Current skill name: KYC Onboarding Review
- Practice area: Financial Crime
- Current description: Use when reviewing a client or investor onboarding packet to inventory documents, extract KYC fields, apply the firm's KYC/AML rules grid, propose a customer risk rating, and assemble an escalation packet for compliance and attorney review.
- Core purpose (must not change): Produce a structured, review-ready draft KYC (Know Your Customer) onboarding file.

CONTEXT TO READ FIRST
- Read skills/financial-crime/kyc-onboarding-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Sanctions Screening Review

- **Skill file:** `skills/financial-crime/sanctions-screening-review/SKILL.md`
- **Practice area:** Financial Crime
- **Current description:** Use when reviewing sanctions, PEP, or adverse-media screening results for named parties to compare identifiers, classify each potential match by confidence, separate likely false positives from genuine hits, and recommend a disposition for compliance and attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/financial-crime/sanctions-screening-review/SKILL.md
- Current skill name: Sanctions Screening Review
- Practice area: Financial Crime
- Current description: Use when reviewing sanctions, PEP, or adverse-media screening results for named parties to compare identifiers, classify each potential match by confidence, separate likely false positives from genuine hits, and recommend a disposition for compliance and attorney review.
- Core purpose (must not change): Produce a structured, review-ready draft adjudication of sanctions, politically exposed person (PEP), and adverse-media screening results.

CONTEXT TO READ FIRST
- Read skills/financial-crime/sanctions-screening-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Legal Ops

### Legal Meeting Briefing

- **Skill file:** `skills/legal-ops/legal-meeting-briefing/SKILL.md`
- **Practice area:** Legal Ops
- **Current description:** Use when preparing a structured briefing for a meeting with legal relevance — assembling background, participants, agenda, key documents, open issues, legal considerations, talking points, and a follow-up action-item tracker from the materials provided.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-ops/legal-meeting-briefing/SKILL.md
- Current skill name: Legal Meeting Briefing
- Practice area: Legal Ops
- Current description: Use when preparing a structured briefing for a meeting with legal relevance — assembling background, participants, agenda, key documents, open issues, legal considerations, talking points, and a follow-up action-item tracker from the materials provided.
- Core purpose (must not change): Produce a structured, review-ready draft briefing for a meeting with legal relevance — a deal review, a board or committee meeting, a vendor call, a regulatory meeting, a litigation discussion, or a cross-functional meeting.

CONTEXT TO READ FIRST
- Read skills/legal-ops/legal-meeting-briefing/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Signature Routing Checklist

- **Skill file:** `skills/legal-ops/signature-routing-checklist/SKILL.md`
- **Practice area:** Legal Ops
- **Current description:** Use when a document is believed final and ready to sign — to run a pre-signature completeness checklist, identify signers and signing order, flag blockers, and produce a routing plan for attorney review before the document is sent for execution.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-ops/signature-routing-checklist/SKILL.md
- Current skill name: Signature Routing Checklist
- Practice area: Legal Ops
- Current description: Use when a document is believed final and ready to sign — to run a pre-signature completeness checklist, identify signers and signing order, flag blockers, and produce a routing plan for attorney review before the document is sent for execution.
- Core purpose (must not change): Produce a draft pre-signature readiness review and signing-routing plan for a document that is believed to be in final form.

CONTEXT TO READ FIRST
- Read skills/legal-ops/signature-routing-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Templated Legal Response

- **Skill file:** `skills/legal-ops/templated-legal-response/SKILL.md`
- **Practice area:** Legal Ops
- **Current description:** Use when drafting a response to a routine, recurring legal inquiry from a configured template, with a built-in escalation gate that stops the templated path when the matter shows characteristics that require individualized counsel review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-ops/templated-legal-response/SKILL.md
- Current skill name: Templated Legal Response
- Practice area: Legal Ops
- Current description: Use when drafting a response to a routine, recurring legal inquiry from a configured template, with a built-in escalation gate that stops the templated path when the matter shows characteristics that require individualized counsel review.
- Core purpose (must not change): Produce a draft response to a routine, recurring legal inquiry using a configured response template, and run an escalation gate before drafting so that matters needing individualized attention do not receive a templated reply.

CONTEXT TO READ FIRST
- Read skills/legal-ops/templated-legal-response/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````
