# AgentCounsel — Skill Improvement Prompts

Generated by `scripts/generate_skill_improvement_prompts.py`. Do not edit this file by hand — re-run the script after adding or changing a skill.

This report contains one copyable improvement prompt for each of the 58 skills in the canonical library (`skills/`). Each prompt asks an AI coding agent to improve one specific skill while preserving its purpose and the AgentCounsel skill structure.

Adapter copies under `adapters/` are generated from the canonical skills and are not listed here. Improve the canonical skill, then run `python scripts/sync_plugin_skills.py` to refresh the bundle.

## How to use these prompts

1. Find the skill you want to improve in the section for its practice area.
2. Copy the entire fenced prompt block for that skill.
3. Paste it into an AI coding agent that has access to this repository.
4. Review the resulting diff with attorney-style scrutiny — keep only the changes that genuinely tighten the skill.
5. Run `python scripts/validate_repo.py` before committing, and regenerate this report so it stays in sync.

For a manual review pass, work through `reports/skill-quality-checklist.md` alongside these prompts.

## Skills by practice area

- [Legal Research](#legal-research) — 1 skill
- [Litigation](#litigation) — 9 skills
- [Contracts](#contracts) — 4 skills
- [Corporate](#corporate) — 6 skills
- [Employment](#employment) — 8 skills
- [Privacy](#privacy) — 4 skills
- [Product Legal](#product-legal) — 4 skills
- [Regulatory](#regulatory) — 3 skills
- [AI Governance](#ai-governance) — 4 skills
- [Intellectual Property](#intellectual-property) — 7 skills
- [Setup](#setup) — 4 skills
- [Legal Methodology](#legal-methodology) — 4 skills

---

## Legal Research

### Legal Research Memo

- **Skill file:** `skills/legal-research/legal-research-memo/SKILL.md`
- **Practice area:** Legal Research
- **Current description:** Use when producing a structured legal research memo in response to a specific legal question, organizing analysis using IRAC discipline (Question Presented, Brief Answer, Facts, Assumptions, Discussion/Analysis, Conclusion) with explicit sourcing requirements and attorney verification checkpoints.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-research/legal-research-memo/SKILL.md
- Current skill name: Legal Research Memo
- Practice area: Legal Research
- Current description: Use when producing a structured legal research memo in response to a specific legal question, organizing analysis using IRAC discipline (Question Presented, Brief Answer, Facts, Assumptions, Discussion/Analysis, Conclusion) with explicit sourcing requirements and attorney verification checkpoints.
- Core purpose (must not change): Produce a structured, attorney-ready legal research memo in response to a discrete legal question.

CONTEXT TO READ FIRST
- Read skills/legal-research/legal-research-memo/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Litigation

### Brief Section Drafter

- **Skill file:** `skills/litigation/brief-section-drafter/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when drafting a single section of a litigation brief — such as a statement of facts, argument section, standard of review, or conclusion — that is cited to the record, consistent with the case theory, and fully flagged for attorney verification before filing.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/brief-section-drafter/SKILL.md
- Current skill name: Brief Section Drafter
- Practice area: Litigation
- Current description: Use when drafting a single section of a litigation brief — such as a statement of facts, argument section, standard of review, or conclusion — that is cited to the record, consistent with the case theory, and fully flagged for attorney verification before filing.
- Core purpose (must not change): Produce a first draft of a single section of a litigation brief for attorney review, revision, and sign-off.

CONTEXT TO READ FIRST
- Read skills/litigation/brief-section-drafter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Claim Chart

- **Skill file:** `skills/litigation/claim-chart/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building an element-by-element claim chart — mapping patent claim limitations or the elements of a civil cause of action or affirmative defense against evidence — to produce a structured gap analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/claim-chart/SKILL.md
- Current skill name: Claim Chart
- Practice area: Litigation
- Current description: Use when building an element-by-element claim chart — mapping patent claim limitations or the elements of a civil cause of action or affirmative defense against evidence — to produce a structured gap analysis for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready claim chart that maps discrete elements — either limitations of a patent claim or the required elements of a civil cause of action or affirmative defense — against the available evidence or accused features.

CONTEXT TO READ FIRST
- Read skills/litigation/claim-chart/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Demand Letter

- **Skill file:** `skills/litigation/demand-letter/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when drafting a demand letter for attorney review — running a structured intake, a pre-draft risk gate, and a draft with every legal conclusion, damages figure, and deadline flagged for attorney confirmation before sending.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/demand-letter/SKILL.md
- Current skill name: Demand Letter
- Practice area: Litigation
- Current description: Use when drafting a demand letter for attorney review — running a structured intake, a pre-draft risk gate, and a draft with every legal conclusion, damages figure, and deadline flagged for attorney confirmation before sending.
- Core purpose (must not change): Produce a structured draft demand letter for attorney review, revision, and sending.

CONTEXT TO READ FIRST
- Read skills/litigation/demand-letter/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Deposition Preparation

- **Skill file:** `skills/litigation/deposition-prep/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building a structured deposition outline from the case theory, witness profile, and documentary record to produce a focused, attorney-ready map for taking or defending a deposition.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/deposition-prep/SKILL.md
- Current skill name: Deposition Preparation
- Practice area: Litigation
- Current description: Use when building a structured deposition outline from the case theory, witness profile, and documentary record to produce a focused, attorney-ready map for taking or defending a deposition.
- Core purpose (must not change): Produce a structured deposition outline for attorney review and use.

CONTEXT TO READ FIRST
- Read skills/litigation/deposition-prep/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Legal Hold

- **Skill file:** `skills/litigation/legal-hold/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when preparing a litigation hold notice and preservation-scope summary — issuing a new hold, refreshing an existing one, or releasing a closed one — as draft legal work product for attorney review before distribution to custodians.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/legal-hold/SKILL.md
- Current skill name: Legal Hold
- Practice area: Litigation
- Current description: Use when preparing a litigation hold notice and preservation-scope summary — issuing a new hold, refreshing an existing one, or releasing a closed one — as draft legal work product for attorney review before distribution to custodians.
- Core purpose (must not change): Produce a structured draft litigation hold notice and preservation-scope summary for attorney review.

CONTEXT TO READ FIRST
- Read skills/litigation/legal-hold/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Litigation Chronology

- **Skill file:** `skills/litigation/litigation-chronology/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when building a factual timeline for litigation from provided source documents, producing a structured chronology table with citations, disputed/undisputed flags, and gap analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/litigation-chronology/SKILL.md
- Current skill name: Litigation Chronology
- Practice area: Litigation
- Current description: Use when building a factual timeline for litigation from provided source documents, producing a structured chronology table with citations, disputed/undisputed flags, and gap analysis for attorney review.
- Core purpose (must not change): Produce a structured, sourced factual chronology of events relevant to a litigation or dispute matter.

CONTEXT TO READ FIRST
- Read skills/litigation/litigation-chronology/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Matter Intake

- **Skill file:** `skills/litigation/matter-intake/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when opening a new litigation or dispute matter to produce a structured intake summary capturing parties, claims, jurisdiction, key dates, and immediate action flags for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/matter-intake/SKILL.md
- Current skill name: Matter Intake
- Practice area: Litigation
- Current description: Use when opening a new litigation or dispute matter to produce a structured intake summary capturing parties, claims, jurisdiction, key dates, and immediate action flags for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready intake summary for a new litigation or dispute matter.

CONTEXT TO READ FIRST
- Read skills/litigation/matter-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privilege Log Review

- **Skill file:** `skills/litigation/privilege-log-review/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when conducting a first-pass review of a privilege log to sort entries into three tiers — confidently privileged, uncertain (flagged for attorney decision), and recommend-remove — so attorney review time focuses on the entries that require judgment.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/privilege-log-review/SKILL.md
- Current skill name: Privilege Log Review
- Practice area: Litigation
- Current description: Use when conducting a first-pass review of a privilege log to sort entries into three tiers — confidently privileged, uncertain (flagged for attorney decision), and recommend-remove — so attorney review time focuses on the entries that require judgment.
- Core purpose (must not change): Produce a structured, attorney-ready first-pass review of a privilege log.

CONTEXT TO READ FIRST
- Read skills/litigation/privilege-log-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Subpoena Triage

- **Skill file:** `skills/litigation/subpoena-triage/SKILL.md`
- **Practice area:** Litigation
- **Current description:** Use when an incoming subpoena has been received to produce a structured triage summary identifying the compliance deadline, scope, objections, privilege issues, preservation obligations, and internal notification requirements for immediate attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/litigation/subpoena-triage/SKILL.md
- Current skill name: Subpoena Triage
- Practice area: Litigation
- Current description: Use when an incoming subpoena has been received to produce a structured triage summary identifying the compliance deadline, scope, objections, privilege issues, preservation obligations, and internal notification requirements for immediate attorney review.
- Core purpose (must not change): Produce a structured triage summary for an incoming subpoena requiring prompt attorney evaluation.

CONTEXT TO READ FIRST
- Read skills/litigation/subpoena-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Contracts

### Contract Risk Review

- **Skill file:** `skills/contracts/contract-risk-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a general commercial contract — such as a master services agreement, vendor agreement, supplier contract, or professional services agreement — to produce a structured risk assessment and prioritized issue list for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/contract-risk-review/SKILL.md
- Current skill name: Contract Risk Review
- Practice area: Contracts
- Current description: Use when reviewing a general commercial contract — such as a master services agreement, vendor agreement, supplier contract, or professional services agreement — to produce a structured risk assessment and prioritized issue list for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready risk review of a commercial contract.

CONTEXT TO READ FIRST
- Read skills/contracts/contract-risk-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### NDA Review

- **Skill file:** `skills/contracts/nda-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a non-disclosure or confidentiality agreement to produce a triage rating (route, flag, or stop), a structured risk summary, and prioritized redline points for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/nda-review/SKILL.md
- Current skill name: NDA Review
- Practice area: Contracts
- Current description: Use when reviewing a non-disclosure or confidentiality agreement to produce a triage rating (route, flag, or stop), a structured risk summary, and prioritized redline points for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a non-disclosure agreement (NDA) or confidentiality agreement.

CONTEXT TO READ FIRST
- Read skills/contracts/nda-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Redline Summary

- **Skill file:** `skills/contracts/redline-summary/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when summarizing the substantive changes between two versions of a contract, a set of tracked changes, or a base agreement plus a series of amendments — to produce a change-by-change table and narrative of the most material shifts for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/redline-summary/SKILL.md
- Current skill name: Redline Summary
- Practice area: Contracts
- Current description: Use when summarizing the substantive changes between two versions of a contract, a set of tracked changes, or a base agreement plus a series of amendments — to produce a change-by-change table and narrative of the most material shifts for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready summary of the substantive edits between two versions of a contract, or within a single document containing tracked changes, or across a base agreement and a series of amendments.

CONTEXT TO READ FIRST
- Read skills/contracts/redline-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### SOW Review

- **Skill file:** `skills/contracts/sow-review/SKILL.md`
- **Practice area:** Contracts
- **Current description:** Use when reviewing a statement of work or work order to assess scope clarity, deliverables, acceptance criteria, timeline, pricing, and — critically — consistency with the governing master agreement.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/contracts/sow-review/SKILL.md
- Current skill name: SOW Review
- Practice area: Contracts
- Current description: Use when reviewing a statement of work or work order to assess scope clarity, deliverables, acceptance criteria, timeline, pricing, and — critically — consistency with the governing master agreement.
- Core purpose (must not change): Produce a structured, attorney-ready review of a statement of work (SOW) or work order (WO).

CONTEXT TO READ FIRST
- Read skills/contracts/sow-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Corporate

### Board Minutes

- **Skill file:** `skills/corporate/board-minutes/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when drafting minutes of a board of directors or board-committee meeting as a corporate record, producing a structured draft for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/board-minutes/SKILL.md
- Current skill name: Board Minutes
- Practice area: Corporate
- Current description: Use when drafting minutes of a board of directors or board-committee meeting as a corporate record, producing a structured draft for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft of minutes for a meeting of a board of directors or a board committee.

CONTEXT TO READ FIRST
- Read skills/corporate/board-minutes/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Closing Checklist

- **Skill file:** `skills/corporate/closing-checklist/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when building or updating a transaction closing checklist — conditions precedent, closing deliverables, and pre-closing actions — and identifying what is blocking a deal closing, as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/closing-checklist/SKILL.md
- Current skill name: Closing Checklist
- Practice area: Corporate
- Current description: Use when building or updating a transaction closing checklist — conditions precedent, closing deliverables, and pre-closing actions — and identifying what is blocking a deal closing, as draft work product for attorney review.
- Core purpose (must not change): Build and maintain a structured transaction closing checklist that captures every condition precedent, closing deliverable, and pre-closing covenant drawn from the transaction agreement.

CONTEXT TO READ FIRST
- Read skills/corporate/closing-checklist/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Diligence Issue Extraction

- **Skill file:** `skills/corporate/diligence-issue-extraction/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when reviewing provided due-diligence documents for an M&A or investment transaction to extract material issues into a structured, severity-sorted issues memo for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/diligence-issue-extraction/SKILL.md
- Current skill name: Diligence Issue Extraction
- Practice area: Corporate
- Current description: Use when reviewing provided due-diligence documents for an M&A or investment transaction to extract material issues into a structured, severity-sorted issues memo for attorney review.
- Core purpose (must not change): Extract and organize material issues from a target's due-diligence documents into a structured issues memo, ready for attorney review.

CONTEXT TO READ FIRST
- Read skills/corporate/diligence-issue-extraction/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Entity Compliance Review

- **Skill file:** `skills/corporate/entity-compliance/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when organizing and reviewing an organization's corporate-entity compliance status — periodic filings, registered-agent status, and good standing — into a structured review that flags overdue, due-soon, and unknown items as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/entity-compliance/SKILL.md
- Current skill name: Entity Compliance Review
- Practice area: Corporate
- Current description: Use when organizing and reviewing an organization's corporate-entity compliance status — periodic filings, registered-agent status, and good standing — into a structured review that flags overdue, due-soon, and unknown items as draft work product for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an organization's corporate-entity compliance posture.

CONTEXT TO READ FIRST
- Read skills/corporate/entity-compliance/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Material Contract Schedule

- **Skill file:** `skills/corporate/material-contract-schedule/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when building the disclosure schedule that lists contracts meeting a transaction agreement's definition of "Material Contract," producing a draft for attorney review before it is delivered as an exhibit.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/material-contract-schedule/SKILL.md
- Current skill name: Material Contract Schedule
- Practice area: Corporate
- Current description: Use when building the disclosure schedule that lists contracts meeting a transaction agreement's definition of "Material Contract," producing a draft for attorney review before it is delivered as an exhibit.
- Core purpose (must not change): Produce a structured, attorney-ready draft of the Material Contract Schedule required as an exhibit to a transaction agreement.

CONTEXT TO READ FIRST
- Read skills/corporate/material-contract-schedule/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Written Consent

- **Skill file:** `skills/corporate/written-consent/SKILL.md`
- **Practice area:** Corporate
- **Current description:** Use when drafting a board or board-committee action by written consent in lieu of a meeting to produce a structured draft for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/corporate/written-consent/SKILL.md
- Current skill name: Written Consent
- Practice area: Corporate
- Current description: Use when drafting a board or board-committee action by written consent in lieu of a meeting to produce a structured draft for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft of a corporate action taken by written consent in lieu of a meeting.

CONTEXT TO READ FIRST
- Read skills/corporate/written-consent/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Employment

### Employee Policy Review

- **Skill file:** `skills/employment/employee-policy-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing an employee handbook or individual workplace policy — or a proposed change to one — for clarity, internal consistency, completeness, and legal risk flags requiring attorney and jurisdiction-specific review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/employee-policy-review/SKILL.md
- Current skill name: Employee Policy Review
- Practice area: Employment
- Current description: Use when reviewing an employee handbook or individual workplace policy — or a proposed change to one — for clarity, internal consistency, completeness, and legal risk flags requiring attorney and jurisdiction-specific review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employee handbook or individual workplace policy, or of a proposed change to an existing handbook or policy.

CONTEXT TO READ FIRST
- Read skills/employment/employee-policy-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Hiring Review

- **Skill file:** `skills/employment/hiring-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing an employment offer letter (and any restrictive-covenant exhibits) against the candidate's actual work jurisdiction and the employer's standard hiring practices to produce a structured draft review memo with a verdict, for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/hiring-review/SKILL.md
- Current skill name: Hiring Review
- Practice area: Employment
- Current description: Use when reviewing an employment offer letter (and any restrictive-covenant exhibits) against the candidate's actual work jurisdiction and the employer's standard hiring practices to produce a structured draft review memo with a verdict, for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employment offer letter and any accompanying restrictive-covenant exhibits.

CONTEXT TO READ FIRST
- Read skills/employment/hiring-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Internal Investigation

- **Skill file:** `skills/employment/internal-investigation/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when supporting an attorney-directed internal investigation to track evidentiary coverage, connect evidence to issues, and draft a privileged investigation memorandum and audience-specific summaries as draft work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/internal-investigation/SKILL.md
- Current skill name: Internal Investigation
- Practice area: Employment
- Current description: Use when supporting an attorney-directed internal investigation to track evidentiary coverage, connect evidence to issues, and draft a privileged investigation memorandum and audience-specific summaries as draft work product for attorney review.
- Core purpose (must not change): Provide structured workflow support for an attorney-directed internal investigation.

CONTEXT TO READ FIRST
- Read skills/employment/internal-investigation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Protected Leave Review

- **Skill file:** `skills/employment/protected-leave-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing and reviewing a protected-leave situation to identify the leave frameworks that may apply, surface procedural obligations and deadlines requiring verified research, and flag adverse-action exposure — producing draft legal work product for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/protected-leave-review/SKILL.md
- Current skill name: Protected Leave Review
- Practice area: Employment
- Current description: Use when organizing and reviewing a protected-leave situation to identify the leave frameworks that may apply, surface procedural obligations and deadlines requiring verified research, and flag adverse-action exposure — producing draft legal work product for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an employee's protected-leave situation.

CONTEXT TO READ FIRST
- Read skills/employment/protected-leave-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Severance Review

- **Skill file:** `skills/employment/severance-review/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when reviewing a severance or separation agreement to produce a structured analysis of consideration, release scope, restrictive covenants, and open legal issues for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/severance-review/SKILL.md
- Current skill name: Severance Review
- Practice area: Employment
- Current description: Use when reviewing a severance or separation agreement to produce a structured analysis of consideration, release scope, restrictive covenants, and open legal issues for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a severance or separation agreement.

CONTEXT TO READ FIRST
- Read skills/employment/severance-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Termination Risk

- **Skill file:** `skills/employment/termination-risk/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing and reviewing the facts surrounding a proposed employee termination so an attorney can assess legal risk before the termination occurs.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/termination-risk/SKILL.md
- Current skill name: Termination Risk
- Practice area: Employment
- Current description: Use when organizing and reviewing the facts surrounding a proposed employee termination so an attorney can assess legal risk before the termination occurs.
- Core purpose (must not change): Produce a structured, attorney-ready fact-and-risk summary for a proposed employee termination.

CONTEXT TO READ FIRST
- Read skills/employment/termination-risk/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Wage and Hour Question Triage

- **Skill file:** `skills/employment/wage-hour-qa/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when a specific wage-and-hour or related employment question — such as overtime eligibility, exemption status, break entitlements, final-pay timing, paid-time-off payout, or leave eligibility — needs to be structured and analyzed against verified, jurisdiction-specific rules, producing a draft analysis for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/wage-hour-qa/SKILL.md
- Current skill name: Wage and Hour Question Triage
- Practice area: Employment
- Current description: Use when a specific wage-and-hour or related employment question — such as overtime eligibility, exemption status, break entitlements, final-pay timing, paid-time-off payout, or leave eligibility — needs to be structured and analyzed against verified, jurisdiction-specific rules, producing a draft analysis for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready draft analysis of a specific wage-and-hour or related employment question.

CONTEXT TO READ FIRST
- Read skills/employment/wage-hour-qa/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Worker Classification

- **Skill file:** `skills/employment/worker-classification/SKILL.md`
- **Practice area:** Employment
- **Current description:** Use when organizing the facts of a proposed worker engagement and structuring an analysis of whether the worker should be classified as an employee or an independent contractor, producing a draft classification analysis memo for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/employment/worker-classification/SKILL.md
- Current skill name: Worker Classification
- Practice area: Employment
- Current description: Use when organizing the facts of a proposed worker engagement and structuring an analysis of whether the worker should be classified as an employee or an independent contractor, producing a draft classification analysis memo for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready classification analysis memo for a **proposed** worker engagement — one that has not yet started.

CONTEXT TO READ FIRST
- Read skills/employment/worker-classification/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Privacy

### DPA Review

- **Skill file:** `skills/privacy/dpa-review/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when reviewing a data processing agreement (DPA) or data processing addendum to produce a structured risk summary and prioritized issues for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/dpa-review/SKILL.md
- Current skill name: DPA Review
- Practice area: Privacy
- Current description: Use when reviewing a data processing agreement (DPA) or data processing addendum to produce a structured risk summary and prioritized issues for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a data processing agreement (DPA) or data processing addendum.

CONTEXT TO READ FIRST
- Read skills/privacy/dpa-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### DSAR Workflow

- **Skill file:** `skills/privacy/dsar-workflow/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when an organization receives a data subject access request (or any data subject rights request) and needs a structured triage, handling record, and response plan for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/dsar-workflow/SKILL.md
- Current skill name: DSAR Workflow
- Practice area: Privacy
- Current description: Use when an organization receives a data subject access request (or any data subject rights request) and needs a structured triage, handling record, and response plan for attorney review.
- Core purpose (must not change): Produce a structured triage record and response plan for a data subject access request (DSAR) or other data subject rights request (deletion, correction, portability, restriction, objection, opt-out, etc.).

CONTEXT TO READ FIRST
- Read skills/privacy/dsar-workflow/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Impact Assessment

- **Skill file:** `skills/privacy/pia-generation/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when drafting an internal privacy impact assessment for a new or changed processing activity to document the data involved, design-linked risks, policy consistency, and mitigations for privacy-counsel review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/pia-generation/SKILL.md
- Current skill name: Privacy Impact Assessment
- Practice area: Privacy
- Current description: Use when drafting an internal privacy impact assessment for a new or changed processing activity to document the data involved, design-linked risks, policy consistency, and mitigations for privacy-counsel review.
- Core purpose (must not change): Produce a structured, attorney-ready draft Privacy Impact Assessment (PIA) for a proposed or recently changed processing activity.

CONTEXT TO READ FIRST
- Read skills/privacy/pia-generation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Policy Gap Review

- **Skill file:** `skills/privacy/privacy-policy-gap-review/SKILL.md`
- **Practice area:** Privacy
- **Current description:** Use when reviewing a published privacy policy or privacy notice to identify gaps, internal inconsistencies, and discrepancies between what the policy says and the organization's described actual data practices.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/privacy/privacy-policy-gap-review/SKILL.md
- Current skill name: Privacy Policy Gap Review
- Practice area: Privacy
- Current description: Use when reviewing a published privacy policy or privacy notice to identify gaps, internal inconsistencies, and discrepancies between what the policy says and the organization's described actual data practices.
- Core purpose (must not change): Produce a structured, attorney-ready gap review of a published privacy policy or privacy notice.

CONTEXT TO READ FIRST
- Read skills/privacy/privacy-policy-gap-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Product Legal

### AI Feature Review

- **Skill file:** `skills/product-legal/ai-feature-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when conducting a legal issue-spotting review for a product feature that uses AI or machine learning to produce a structured issues register and recommendations for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/ai-feature-review/SKILL.md
- Current skill name: AI Feature Review
- Practice area: Product Legal
- Current description: Use when conducting a legal issue-spotting review for a product feature that uses AI or machine learning to produce a structured issues register and recommendations for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready legal issues register for a product feature that uses artificial intelligence or machine learning.

CONTEXT TO READ FIRST
- Read skills/product-legal/ai-feature-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Launch Review

- **Skill file:** `skills/product-legal/launch-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when conducting a pre-launch legal issue-spotting review for a new product or feature to produce a structured issues register and a draft go/hold/conditions recommendation for attorney sign-off.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/launch-review/SKILL.md
- Current skill name: Launch Review
- Practice area: Product Legal
- Current description: Use when conducting a pre-launch legal issue-spotting review for a new product or feature to produce a structured issues register and a draft go/hold/conditions recommendation for attorney sign-off.
- Core purpose (must not change): Produce a structured, attorney-ready legal issues register for a product or feature before it launches.

CONTEXT TO READ FIRST
- Read skills/product-legal/launch-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Marketing Claims Review

- **Skill file:** `skills/product-legal/marketing-claims-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when reviewing marketing or advertising copy for legal risk to produce a claim-by-claim analysis, substantiation requirements, and flagged disclosures for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/marketing-claims-review/SKILL.md
- Current skill name: Marketing Claims Review
- Practice area: Product Legal
- Current description: Use when reviewing marketing or advertising copy for legal risk to produce a claim-by-claim analysis, substantiation requirements, and flagged disclosures for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of marketing or advertising copy.

CONTEXT TO READ FIRST
- Read skills/product-legal/marketing-claims-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Terms of Service Review

- **Skill file:** `skills/product-legal/terms-of-service-review/SKILL.md`
- **Practice area:** Product Legal
- **Current description:** Use when reviewing a terms of service or terms of use document to produce a structured risk summary, issues table, and prioritized recommendations for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/product-legal/terms-of-service-review/SKILL.md
- Current skill name: Terms of Service Review
- Practice area: Product Legal
- Current description: Use when reviewing a terms of service or terms of use document to produce a structured risk summary, issues table, and prioritized recommendations for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of a terms of service (ToS) or terms of use (ToU) document, for either a consumer (B2C) or business-to-business (B2B) context.

CONTEXT TO READ FIRST
- Read skills/product-legal/terms-of-service-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Regulatory

### Compliance Gap Matrix

- **Skill file:** `skills/regulatory/compliance-gap-matrix/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when mapping a set of regulatory or framework requirements against an organization's current controls to surface gaps, prioritize remediation, and produce an attorney-ready draft gap analysis.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/compliance-gap-matrix/SKILL.md
- Current skill name: Compliance Gap Matrix
- Practice area: Regulatory
- Current description: Use when mapping a set of regulatory or framework requirements against an organization's current controls to surface gaps, prioritize remediation, and produce an attorney-ready draft gap analysis.
- Core purpose (must not change): Produce a structured, attorney-ready draft compliance gap matrix that maps discrete regulatory or framework requirements against the organization's described current controls and practices.

CONTEXT TO READ FIRST
- Read skills/regulatory/compliance-gap-matrix/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Enforcement Risk Memo

- **Skill file:** `skills/regulatory/enforcement-risk-memo/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when structuring a memo that assesses potential enforcement exposure for a described practice, conduct, or set of facts, to produce attorney-ready draft analysis for review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/enforcement-risk-memo/SKILL.md
- Current skill name: Enforcement Risk Memo
- Practice area: Regulatory
- Current description: Use when structuring a memo that assesses potential enforcement exposure for a described practice, conduct, or set of facts, to produce attorney-ready draft analysis for review.
- Core purpose (must not change): Produce a structured, attorney-ready draft memo assessing potential regulatory enforcement exposure for a described situation, conduct, or practice.

CONTEXT TO READ FIRST
- Read skills/regulatory/enforcement-risk-memo/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Rule Change Summary

- **Skill file:** `skills/regulatory/rule-change-summary/SKILL.md`
- **Practice area:** Regulatory
- **Current description:** Use when summarizing a regulatory rule change, proposed rule, or official guidance document and its practical impact on an organization, based on the actual document provided.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/regulatory/rule-change-summary/SKILL.md
- Current skill name: Rule Change Summary
- Practice area: Regulatory
- Current description: Use when summarizing a regulatory rule change, proposed rule, or official guidance document and its practical impact on an organization, based on the actual document provided.
- Core purpose (must not change): Produce a structured, attorney-ready summary of a regulatory rule change, proposed rule, interim final rule, or official guidance document, and identify its practical implications for a specific organization or practice area.

CONTEXT TO READ FIRST
- Read skills/regulatory/rule-change-summary/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## AI Governance

### AI Use Case Intake

- **Skill file:** `skills/ai-governance/ai-use-case-intake/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when a new or modified AI/ML use case needs to be documented and triaged so legal, compliance, and governance teams can assess risk and route it to the right specialists.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/ai-use-case-intake/SKILL.md
- Current skill name: AI Use Case Intake
- Practice area: AI Governance
- Current description: Use when a new or modified AI/ML use case needs to be documented and triaged so legal, compliance, and governance teams can assess risk and route it to the right specialists.
- Core purpose (must not change): Produce a structured, attorney-ready intake record for a proposed or in-flight AI/ML use case.

CONTEXT TO READ FIRST
- Read skills/ai-governance/ai-use-case-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### AI Vendor Terms Review

- **Skill file:** `skills/ai-governance/ai-vendor-terms-review/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when reviewing the terms of service, API agreement, or usage policies of an AI vendor or AI-enabled service to produce a structured risk summary and prioritized redline points for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/ai-vendor-terms-review/SKILL.md
- Current skill name: AI Vendor Terms Review
- Practice area: AI Governance
- Current description: Use when reviewing the terms of service, API agreement, or usage policies of an AI vendor or AI-enabled service to produce a structured risk summary and prioritized redline points for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of an AI vendor's terms of service, API agreement, enterprise license, or acceptable use policy.

CONTEXT TO READ FIRST
- Read skills/ai-governance/ai-vendor-terms-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Employee AI Policy

- **Skill file:** `skills/ai-governance/employee-ai-policy/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when reviewing a draft internal employee AI-use policy — or drafting review criteria when no policy exists — to identify gaps, inconsistencies, and priority issues for attorney and HR review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/employee-ai-policy/SKILL.md
- Current skill name: Employee AI Policy
- Practice area: AI Governance
- Current description: Use when reviewing a draft internal employee AI-use policy — or drafting review criteria when no policy exists — to identify gaps, inconsistencies, and priority issues for attorney and HR review.
- Core purpose (must not change): Produce a structured review of an organization's internal employee AI-use policy (or, if no policy exists, a structured gap analysis based on the topics a policy should address).

CONTEXT TO READ FIRST
- Read skills/ai-governance/employee-ai-policy/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Model Risk Triage

- **Skill file:** `skills/ai-governance/model-risk-triage/SKILL.md`
- **Practice area:** AI Governance
- **Current description:** Use when triaging the legal and governance risk of a specific AI model or AI system before or during deployment, to produce a structured risk register and recommended controls for attorney and governance review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ai-governance/model-risk-triage/SKILL.md
- Current skill name: Model Risk Triage
- Practice area: AI Governance
- Current description: Use when triaging the legal and governance risk of a specific AI model or AI system before or during deployment, to produce a structured risk register and recommended controls for attorney and governance review.
- Core purpose (must not change): Produce a structured triage of the legal, governance, and operational risk dimensions of a specific AI model or AI system.

CONTEXT TO READ FIRST
- Read skills/ai-governance/model-risk-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Intellectual Property

### Cease and Desist Response

- **Skill file:** `skills/ip/cease-and-desist-response/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a client has received a cease-and-desist letter or similar IP demand and needs a structured triage memo — covering the assertion, exposure, response options, and immediate actions — for attorney review before responding.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/cease-and-desist-response/SKILL.md
- Current skill name: Cease and Desist Response
- Practice area: Intellectual Property
- Current description: Use when a client has received a cease-and-desist letter or similar IP demand and needs a structured triage memo — covering the assertion, exposure, response options, and immediate actions — for attorney review before responding.
- Core purpose (must not change): Produce a structured triage memo for a client who has received a cease-and-desist letter or similar intellectual property demand.

CONTEXT TO READ FIRST
- Read skills/ip/cease-and-desist-response/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### DMCA Takedown

- **Skill file:** `skills/ip/dmca-takedown/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when preparing a takedown notice for infringing content or evaluating a takedown notice received against the user's content, including whether a counter-notice may be appropriate.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/dmca-takedown/SKILL.md
- Current skill name: DMCA Takedown
- Practice area: Intellectual Property
- Current description: Use when preparing a takedown notice for infringing content or evaluating a takedown notice received against the user's content, including whether a counter-notice may be appropriate.
- Core purpose (must not change): Produce a structured draft notice or counter-notice under the Digital Millennium Copyright Act (DMCA) notice-and-takedown framework, or evaluate a notice received against the user's content.

CONTEXT TO READ FIRST
- Read skills/ip/dmca-takedown/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Infringement Triage

- **Skill file:** `skills/ip/infringement-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a client needs a first-pass, structured triage of a potential intellectual property infringement issue — identifying the key factors, flagging their direction, and routing to IP counsel — without concluding whether infringement occurred.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/infringement-triage/SKILL.md
- Current skill name: Infringement Triage
- Practice area: Intellectual Property
- Current description: Use when a client needs a first-pass, structured triage of a potential intellectual property infringement issue — identifying the key factors, flagging their direction, and routing to IP counsel — without concluding whether infringement occurred.
- Core purpose (must not change): Produce a structured first-pass triage memo for a potential intellectual property infringement issue.

CONTEXT TO READ FIRST
- Read skills/ip/infringement-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Invention Disclosure Intake

- **Skill file:** `skills/ip/invention-intake/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user wants a structured first-pass screen of a proposed invention to assess whether it warrants patent-counsel attention, with filing-deadline urgency flagging.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/invention-intake/SKILL.md
- Current skill name: Invention Disclosure Intake
- Practice area: Intellectual Property
- Current description: Use when a user wants a structured first-pass screen of a proposed invention to assess whether it warrants patent-counsel attention, with filing-deadline urgency flagging.
- Core purpose (must not change): Produce a structured first-pass invention disclosure screen that helps patent counsel quickly orient to a proposed invention, identify obvious patentability signals, and flag any urgent filing-deadline concerns.

CONTEXT TO READ FIRST
- Read skills/ip/invention-intake/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Open Source License Review

- **Skill file:** `skills/ip/open-source-license-review/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when reviewing the open-source licenses present in or proposed for a project to identify compliance obligations and flag compatibility or disclosure risks for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/open-source-license-review/SKILL.md
- Current skill name: Open Source License Review
- Practice area: Intellectual Property
- Current description: Use when reviewing the open-source licenses present in or proposed for a project to identify compliance obligations and flag compatibility or disclosure risks for attorney review.
- Core purpose (must not change): Produce a structured, attorney-ready review of the open-source licenses present in a software project or proposed for adoption.

CONTEXT TO READ FIRST
- Read skills/ip/open-source-license-review/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Patent Freedom-to-Operate Triage

- **Skill file:** `skills/ip/fto-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user needs a structured first-pass review of potential patent blocking issues for a product, process, or feature against a set of identified patents, to flag risk areas and route findings to patent counsel for a formal freedom-to-operate opinion.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/fto-triage/SKILL.md
- Current skill name: Patent Freedom-to-Operate Triage
- Practice area: Intellectual Property
- Current description: Use when a user needs a structured first-pass review of potential patent blocking issues for a product, process, or feature against a set of identified patents, to flag risk areas and route findings to patent counsel for a formal freedom-to-operate opinion.
- Core purpose (must not change): Produce a structured, attorney-ready triage memo that identifies potential patent blocking issues for a described product, process, or feature against a specific set of patents the user has already identified.

CONTEXT TO READ FIRST
- Read skills/ip/fto-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Trademark Clearance Triage

- **Skill file:** `skills/ip/trademark-clearance-triage/SKILL.md`
- **Practice area:** Intellectual Property
- **Current description:** Use when a user wants a preliminary triage of a proposed brand name, product name, or logo before engaging trademark counsel for a formal clearance search.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/ip/trademark-clearance-triage/SKILL.md
- Current skill name: Trademark Clearance Triage
- Practice area: Intellectual Property
- Current description: Use when a user wants a preliminary triage of a proposed brand name, product name, or logo before engaging trademark counsel for a formal clearance search.
- Core purpose (must not change): Produce a structured preliminary triage of a proposed trademark — a brand name, product name, slogan, or logo — to identify obvious concerns and orient the client before engaging trademark counsel for a formal clearance search and opinion.

CONTEXT TO READ FIRST
- Read skills/ip/trademark-clearance-triage/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Setup

### Contracts Cold-Start Interview

- **Skill file:** `skills/setup/contracts-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a contracts practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/contracts-cold-start-interview/SKILL.md
- Current skill name: Contracts Cold-Start Interview
- Practice area: Setup
- Current description: Use when a contracts practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a contracts practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/contracts.md`.

CONTEXT TO READ FIRST
- Read skills/setup/contracts-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Corporate Cold-Start Interview

- **Skill file:** `skills/setup/corporate-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a corporate practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/corporate-cold-start-interview/SKILL.md
- Current skill name: Corporate Cold-Start Interview
- Practice area: Setup
- Current description: Use when a corporate practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a corporate practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/corporate.md`.

CONTEXT TO READ FIRST
- Read skills/setup/corporate-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Litigation Cold-Start Interview

- **Skill file:** `skills/setup/litigation-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a litigation practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/litigation-cold-start-interview/SKILL.md
- Current skill name: Litigation Cold-Start Interview
- Practice area: Setup
- Current description: Use when a litigation practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a litigation practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/litigation.md`.

CONTEXT TO READ FIRST
- Read skills/setup/litigation-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Privacy Cold-Start Interview

- **Skill file:** `skills/setup/privacy-cold-start-interview/SKILL.md`
- **Practice area:** Setup
- **Current description:** Use when a privacy practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/setup/privacy-cold-start-interview/SKILL.md
- Current skill name: Privacy Cold-Start Interview
- Practice area: Setup
- Current description: Use when a privacy practice group is adopting AgentCounsel and needs to configure its practice profile by answering a structured interview covering jurisdictions, client context, escalation thresholds, output preferences, source documents, standard positions, review requirements, and prohibited assumptions.
- Core purpose (must not change): Conduct a structured, staged interview with a privacy practice group — led by a supervising attorney or authorized designee — to gather the information required to populate `practice-profiles/privacy.md`.

CONTEXT TO READ FIRST
- Read skills/setup/privacy-cold-start-interview/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

## Legal Methodology

### Red-Team Verifier

- **Skill file:** `skills/legal-methodology/red-team-verifier/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when checking whether a legal memo, contract review, demand letter, risk matrix, research summary, draft filing, or other legal AI output is reliable enough to rely on — adversarially stress-testing it before attorney review to surface invented authority, unsupported claims, weak reasoning, jurisdiction and deadline gaps, and professional-responsibility issues.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/red-team-verifier/SKILL.md
- Current skill name: Red-Team Verifier
- Practice area: Legal Methodology
- Current description: Use when checking whether a legal memo, contract review, demand letter, risk matrix, research summary, draft filing, or other legal AI output is reliable enough to rely on — adversarially stress-testing it before attorney review to surface invented authority, unsupported claims, weak reasoning, jurisdiction and deadline gaps, and professional-responsibility issues.
- Core purpose (must not change): Adversarially stress-test a legal work product before anyone relies on it.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/red-team-verifier/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Risk Assessment

- **Skill file:** `skills/legal-methodology/risk-assessment/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when applying a structured, jurisdiction-agnostic method to identify, rate, and prioritize legal risks in a situation, producing a risk register with severity bands and a prioritized summary for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/risk-assessment/SKILL.md
- Current skill name: Risk Assessment
- Practice area: Legal Methodology
- Current description: Use when applying a structured, jurisdiction-agnostic method to identify, rate, and prioritize legal risks in a situation, producing a risk register with severity bands and a prioritized summary for attorney review.
- Core purpose (must not change): Apply a structured, jurisdiction-agnostic method for identifying, rating, and prioritizing legal risk across a situation, transaction, document, or matter.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/risk-assessment/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Source Validation

- **Skill file:** `skills/legal-methodology/source-validation/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed, applying the source hierarchy in `core/source-and-citation-discipline.md`.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/source-validation/SKILL.md
- Current skill name: Source Validation
- Practice area: Legal Methodology
- Current description: Use when checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed, applying the source hierarchy in `core/source-and-citation-discipline.md`.
- Core purpose (must not change): Apply a systematic method for checking that every cited source, authority, quotation, date, and factual claim in a draft legal work product actually exists and says what is claimed.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/source-validation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````

### Statutory Interpretation

- **Skill file:** `skills/legal-methodology/statutory-interpretation/SKILL.md`
- **Practice area:** Legal Methodology
- **Current description:** Use when applying a disciplined analytical method to parse and interpret a statutory, regulatory, or contractual provision — mapping structure, defined terms, and competing readings — to produce a structured interpretation worksheet for attorney review.
- **Structure check:** all 8 required sections present.

````text
You are improving a single skill in AgentCounsel, an open, Markdown-native
library of legal workflows for AI agents and the attorneys who supervise
them. Every skill is a SKILL.md file that produces draft legal work product
for attorney review — never a final legal opinion.

TARGET SKILL
- Skill path: skills/legal-methodology/statutory-interpretation/SKILL.md
- Current skill name: Statutory Interpretation
- Practice area: Legal Methodology
- Current description: Use when applying a disciplined analytical method to parse and interpret a statutory, regulatory, or contractual provision — mapping structure, defined terms, and competing readings — to produce a structured interpretation worksheet for attorney review.
- Core purpose (must not change): Apply a disciplined, jurisdiction-agnostic analytical method for working through a statutory, regulatory, or contractual provision.

CONTEXT TO READ FIRST
- Read skills/legal-methodology/statutory-interpretation/SKILL.md in full.
- Read the shared operating rules in core/ — they apply to every skill.
- Read any references/ folder in the same practice area, and any files in
  this skill's templates/ folder, so your edits stay consistent with them.

TASK
Revise the target skill file in place so it is clearer, tighter, and more
reliable to execute. Improve only this one skill. Do not touch other files
except to keep cross-references accurate.

IMPROVEMENT GOALS
- Make it faster and less ambiguous for an agent to decide whether this
  skill applies to a given request.
- Make the skill produce a more consistent, better-organized, attorney-ready
  result every time it runs.
- Make the safety posture unmistakable: the output is draft work product for
  attorney review, built only on sources the user actually supplied.

SPECIFIC REQUIREMENTS
1. Strengthen trigger clarity. Sharpen "Use When" and "Do Not Use When" so
   the skill's scope is unambiguous and each trigger is concrete and
   testable. Resolve any overlap with neighboring skills by naming them and
   their paths.
2. Strengthen required inputs. Make "Required Inputs" complete and specific:
   every input the workflow depends on must be listed, and optional-but-
   recommended inputs marked as such. State explicitly that when a required
   input is missing the skill stops and requests it, and never reconstructs
   or assumes the missing material.
3. Improve the workflow steps. Make every step in "Workflow" concrete,
   ordered, and checkable. Remove vague or redundant steps and merge
   duplicates. Ensure every deliverable promised by "Output Format" is
   produced by a step, and that no step produces something the output never
   reports.
4. Improve the output format. Make "Output Format" match the workflow
   exactly — every deliverable section named, ordered, and described. Require
   visible placeholders for missing information instead of invented content,
   and define any rating or triage label where it is used.
5. Strengthen attorney-review and source-discipline language. Reinforce
   "Legal Safety Rules" and the "Attorney Verification Checklist": attorney
   review is always required; never invent legal authority, citations,
   quotations, facts, market data, or deadlines; keep fact, assumption, law,
   analysis, and verification items separate; flag every point of
   uncertainty rather than resolving it silently.

REQUIRED CONSTRAINTS
- Do NOT change the core purpose of this skill. It must still do exactly the
  job stated under "Core purpose" above. Reorganizing, tightening, and
  clarifying are welcome; repurposing the skill is not.
- Preserve the AgentCounsel skill structure. Keep the YAML frontmatter with
  a non-empty `name` and `description`, and keep these H2 sections in this
  exact order: Purpose, Use When, Required Inputs, Do Not Use When, Legal
  Safety Rules, Workflow, Output Format, Attorney Verification Checklist.
- Keep `description` a single line that still begins by describing when to
  use the skill.
- Keep the skill Markdown-only: no build steps, no runtime, no new
  dependencies.
- Do not add jurisdiction-specific legal rules or legal conclusions. The
  skill supplies process and structure, not the law itself.
- Keep relative paths to shared references/ material and to templates/
  accurate; do not break links.

WHEN YOU ARE DONE
- Run `python scripts/validate_repo.py` and confirm it passes.
- Summarize what you changed, section by section, and why each change makes
  the skill clearer or safer.
````
